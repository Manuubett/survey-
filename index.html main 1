<!DOCTYPE html>
<html lang="en">
<head>
<title>Embu County Political Survey – ECS-2025</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta charset="UTF-8">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600;700&family=DM+Serif+Display&display=swap" rel="stylesheet">
<script src="https://www.gstatic.com/firebasejs/9.6.1/firebase-app-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/9.6.1/firebase-firestore-compat.js"></script>
<style>
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
:root {
  --navy:    #0f2547;
  --blue:    #1255cc;
  --sky:     #3b82f6;
  --green:   #16a34a;
  --amber:   #d97706;
  --red:     #dc2626;
  --bg:      #f4f6fb;
  --surface: #ffffff;
  --border:  #e2e8f4;
  --text:    #1e293b;
  --muted:   #64748b;
  --radius:  10px;
  --shadow:  0 2px 12px rgba(15,37,71,.08);
}
body { font-family: 'DM Sans', sans-serif; background: var(--bg); color: var(--text); line-height: 1.55; font-size: 14px; }

.site-header { background: linear-gradient(135deg, var(--navy) 0%, #1a3fa0 100%); color: #fff; padding: 32px 20px 24px; text-align: center; position: relative; overflow: hidden; }
.site-header::before { content: ''; position: absolute; inset: 0; background: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.04'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E"); }
.site-header h1 { font-family: 'DM Serif Display', serif; font-size: clamp(20px, 5vw, 28px); letter-spacing: .5px; position: relative; }
.site-header p { font-size: 13px; opacity: .8; margin-top: 4px; position: relative; }
.header-badge { display: inline-block; margin-top: 12px; background: rgba(255,255,255,.15); border: 1px solid rgba(255,255,255,.3); padding: 4px 16px; border-radius: 20px; font-size: 11px; letter-spacing: 1.2px; text-transform: uppercase; position: relative; }
.header-actions { margin-top: 16px; position: relative; }
.header-btn { background: rgba(255,255,255,.18); border: 1.5px solid rgba(255,255,255,.4); color: #fff; padding: 8px 22px; border-radius: 24px; font-size: 13px; font-weight: 600; cursor: pointer; font-family: inherit; transition: all .2s; letter-spacing: .3px; }
.header-btn:hover { background: rgba(255,255,255,.28); }

.status-strip { background: var(--surface); border-bottom: 1px solid var(--border); padding: 8px 20px; display: none; }
.status-strip-inner { max-width: 780px; margin: 0 auto; display: flex; align-items: center; gap: 10px; flex-wrap: wrap; font-size: 12.5px; }
.gps-dot { width: 9px; height: 9px; border-radius: 50%; background: #bbb; flex-shrink: 0; transition: background .3s; }
.gps-dot.acquiring { background: var(--amber); animation: pulse 1.2s infinite; }
.gps-dot.good      { background: var(--green); }
.gps-dot.poor      { background: var(--red); }
@keyframes pulse { 0%,100%{opacity:1}50%{opacity:.35} }

#draftBar { display: none; position: sticky; top: 0; z-index: 200; background: #fffbeb; border-bottom: 2px solid #fcd34d; padding: 8px 16px; }
.draft-inner { max-width: 780px; margin: 0 auto; display: flex; align-items: center; justify-content: space-between; gap: 10px; flex-wrap: wrap; font-size: 13px; }
.draft-left { display: flex; align-items: center; gap: 8px; }
.draft-text { color: #78350f; font-weight: 600; }
.draft-sub  { color: #92400e; font-size: 11.5px; }
.draft-btn { font-size: 11.5px; font-weight: 700; padding: 5px 14px; border-radius: 20px; cursor: pointer; border: 1.5px solid; font-family: inherit; transition: all .15s; }
.draft-btn.restore { background: var(--blue); color: #fff; border-color: var(--blue); }
.draft-btn.discard { background: #fff; color: var(--red); border-color: #fca5a5; }

#proximityLockBanner { display: none; background: #fff1f2; border-bottom: 2px solid #fecdd3; padding: 9px 20px; position: sticky; top: 0; z-index: 199; }
.lock-inner { max-width: 780px; margin: 0 auto; display: flex; align-items: center; gap: 10px; font-size: 13px; color: #991b1b; font-weight: 600; }

#networkBadge { display: none; position: fixed; bottom: 20px; right: 20px; z-index: 999; padding: 10px 18px; border-radius: 30px; font-size: 13px; font-weight: 700; box-shadow: 0 4px 20px rgba(0,0,0,.2); pointer-events: none; transition: all .3s; align-items: center; gap: 7px; }
#networkBadge.offline { background: var(--red); color: #fff; display: flex; }
#networkBadge.online  { background: var(--green); color: #fff; display: flex; }

#autosaveIndicator { position: fixed; bottom: 72px; right: 20px; z-index: 998; font-size: 11.5px; color: #555; background: #fff; border: 1px solid var(--border); border-radius: 20px; padding: 5px 13px; box-shadow: var(--shadow); display: none; transition: opacity .4s; }

#surveyWrapper { display: none; }
.container { max-width: 780px; margin: 0 auto; padding: 22px 16px 60px; }

#researcherBanner { background: #f0fdf4; border: 1px solid #bbf7d0; border-left: 5px solid var(--green); border-radius: var(--radius); padding: 11px 16px; margin-bottom: 18px; display: none; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 8px; font-size: 13px; color: #166534; }
.res-action-btn { font-size: 11px; padding: 5px 13px; border: 1px solid var(--green); background: #fff; color: var(--green); border-radius: 20px; cursor: pointer; font-weight: 700; font-family: inherit; transition: all .15s; }
.res-action-btn:hover { background: var(--green); color: #fff; }
.res-action-btn.pending { background: var(--amber); color: #fff; border-color: var(--amber); }

#pendingBanner { display: none; background: #fef3c7; border: 1.5px solid #fcd34d; border-left: 5px solid var(--amber); border-radius: var(--radius); padding: 11px 16px; margin-bottom: 16px; font-size: 13px; color: #78350f; }
.sync-btn { display: inline-block; margin-top: 7px; background: var(--amber); color: #fff; border: none; border-radius: 20px; padding: 5px 18px; font-size: 12px; font-weight: 700; cursor: pointer; font-family: inherit; }

.card { background: var(--surface); border-radius: 12px; box-shadow: var(--shadow); margin-bottom: 22px; overflow: hidden; border: 1px solid var(--border); }
.card-head { background: linear-gradient(135deg, var(--navy), #1a3fa0); color: #fff; padding: 13px 20px; display: flex; align-items: center; gap: 12px; }
.card-head.green { background: linear-gradient(135deg, #166534, #16a34a); }
.section-badge { background: rgba(255,255,255,.2); border-radius: 50%; width: 30px; height: 30px; display: flex; align-items: center; justify-content: center; font-weight: 700; font-size: 13px; flex-shrink: 0; }
.card-head h3 { font-size: 13px; letter-spacing: .6px; text-transform: uppercase; font-weight: 600; }
.card-body { padding: 20px; }

.field { margin-bottom: 18px; }
.field:last-child { margin-bottom: 0; }
.field-label { display: block; font-weight: 600; font-size: 13px; color: var(--text); margin-bottom: 7px; }
.field-label .qn { color: var(--blue); margin-right: 4px; }
.field-note { font-size: 11px; color: var(--muted); font-style: italic; margin-left: 4px; }

select, input[type="text"], textarea { width: 100%; padding: 10px 13px; border: 1.5px solid var(--border); border-radius: 8px; font-size: 14px; background: #fafbff; color: var(--text); transition: border-color .2s, box-shadow .2s; font-family: inherit; }
select:focus, input[type="text"]:focus, textarea:focus { outline: none; border-color: var(--sky); background: #fff; box-shadow: 0 0 0 3px rgba(59,130,246,.12); }
textarea { min-height: 76px; resize: vertical; }

.pill-row { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 5px; }
.pill { position: relative; }
.pill input { position: absolute; opacity: 0; width: 0; height: 0; }
.pill label { display: inline-block; padding: 7px 15px; border: 1.5px solid var(--border); border-radius: 24px; font-size: 13px; cursor: pointer; color: var(--muted); background: #fafbff; transition: all .15s; user-select: none; }
.pill input:checked + label { background: var(--blue); color: #fff; border-color: var(--blue); font-weight: 600; }

.svc-table { width: 100%; border-collapse: collapse; font-size: 13px; }
.svc-table th, .svc-table td { padding: 9px 8px; border: 1px solid var(--border); text-align: center; }
.svc-table th { background: #eef2ff; color: var(--navy); font-size: 11.5px; font-weight: 600; }
.svc-table td:first-child { text-align: left; font-weight: 600; color: var(--text); }
.svc-table tr:nth-child(even) td { background: #fafbff; }
.svc-table input[type="radio"] { width: 16px; height: 16px; accent-color: var(--blue); cursor: pointer; }

.gov-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
@media(max-width:480px) { .gov-grid { grid-template-columns: 1fr; } }
.gov-card { border: 2px solid var(--border); border-radius: 10px; padding: 14px; cursor: pointer; transition: all .15s; text-align: center; }
.gov-card:hover { border-color: var(--sky); background: #f0f8ff; }
.gov-card.selected { border-color: var(--blue); background: #eef3ff; }
.gov-card input { display: none; }
.gov-card .gc-name { font-weight: 700; font-size: 14px; color: var(--navy); }
.gov-card .gc-tag  { font-size: 11px; color: var(--muted); margin-top: 3px; }

.two-col { display: grid; grid-template-columns: 1fr 1fr; gap: 14px; }
@media(max-width:500px) { .two-col { grid-template-columns: 1fr; } }

.section-note { font-size: 12px; color: var(--muted); font-style: italic; margin-bottom: 13px; }
.opt-badge { display: inline-block; background: #f0fdf4; color: var(--green); font-size: 10.5px; font-weight: 700; padding: 2px 9px; border-radius: 10px; margin-left: 8px; }

.submit-wrap { margin-top: 8px; }
#submitBtn { width: 100%; padding: 15px; background: linear-gradient(135deg, var(--blue), var(--navy)); color: #fff; border: none; border-radius: 10px; font-size: 16px; font-weight: 700; cursor: pointer; letter-spacing: .4px; transition: opacity .2s; font-family: inherit; }
#submitBtn:disabled { opacity: .4; cursor: not-allowed; }
#submitBtn:not(:disabled):hover { opacity: .88; }

.status-msg { text-align: center; margin-top: 14px; font-size: 14px; font-weight: 600; padding: 11px 16px; border-radius: 8px; }
.status-msg.success { color: #166534; background: #dcfce7; border: 1px solid #86efac; }
.status-msg.error   { color: #991b1b; background: #fee2e2; border: 1px solid #fca5a5; }
.status-msg.warning { color: #92400e; background: #fef3c7; border: 1px solid #fcd34d; }
.status-msg.offline { color: #1e40af; background: #dbeafe; border: 1px solid #93c5fd; }

.loc-box { background: #f8faff; border: 1.5px solid #c7d7f9; border-radius: 10px; padding: 14px 16px; margin-top: 14px; font-size: 13px; }
.loc-box-head { font-weight: 700; color: var(--navy); margin-bottom: 8px; display: flex; align-items: center; gap: 8px; }
.loc-row { display: flex; flex-wrap: wrap; gap: 6px 16px; font-size: 12.5px; color: var(--muted); }
.loc-val { font-weight: 600; color: var(--text); }
.loc-acc { font-weight: 700; }
.loc-acc.good { color: var(--green); }
.loc-acc.poor { color: var(--red); }

.modal-overlay { position: fixed; inset: 0; background: rgba(10,20,50,.65); z-index: 1000; display: flex; align-items: center; justify-content: center; padding: 16px; backdrop-filter: blur(3px); }
.modal-overlay.hidden { display: none; }
.modal-box { background: #fff; border-radius: 16px; width: 100%; max-width: 660px; max-height: 92vh; overflow: hidden; display: flex; flex-direction: column; box-shadow: 0 32px 80px rgba(0,0,0,.25); animation: mIn .22s ease; }
@keyframes mIn { from{transform:scale(.93);opacity:0} to{transform:scale(1);opacity:1} }
.modal-head { background: linear-gradient(135deg, var(--navy), #1a3fa0); color: #fff; padding: 22px 26px; flex-shrink: 0; }
.modal-head h2 { font-size: 19px; font-family: 'DM Serif Display', serif; margin-bottom: 3px; }
.modal-head p  { font-size: 12px; opacity: .8; font-style: italic; }
.modal-scroll  { overflow-y: auto; padding: 22px 26px; flex: 1; }
.modal-foot    { padding: 16px 26px; border-top: 2px solid var(--border); background: #f8faff; flex-shrink: 0; }

.consent-h4 { color: var(--navy); font-size: 12.5px; text-transform: uppercase; letter-spacing: .5px; margin: 18px 0 6px; }
.consent-h4:first-child { margin-top: 0; }
.consent-section p, .consent-section li { font-size: 13px; color: #444; line-height: 1.75; margin: 2px 0; }
.consent-section ul { padding-left: 20px; margin: 4px 0; }
.consent-check { display: flex; align-items: flex-start; gap: 11px; margin-bottom: 18px; background: #fffbeb; border: 1px solid #fde68a; border-radius: 8px; padding: 13px 15px; }
.consent-check input[type="checkbox"] { width: 18px; height: 18px; margin-top: 2px; flex-shrink: 0; accent-color: var(--blue); cursor: pointer; }
.consent-check label { font-size: 13px; color: #333; font-weight: 600; cursor: pointer; line-height: 1.55; }
.btn-row { display: flex; gap: 10px; justify-content: flex-end; }
.btn-decline { padding: 10px 22px; border: 1.5px solid #ccc; border-radius: 8px; background: #fff; color: var(--muted); font-size: 14px; cursor: pointer; font-weight: 700; font-family: inherit; transition: all .15s; }
.btn-decline:hover { background: #f5f5f5; }
.btn-proceed { padding: 10px 28px; border: none; border-radius: 8px; background: linear-gradient(135deg, var(--blue), var(--navy)); color: #fff; font-size: 14px; cursor: pointer; font-weight: 700; font-family: inherit; transition: opacity .15s; }
.btn-proceed:disabled { opacity: .35; cursor: not-allowed; }
.btn-proceed:not(:disabled):hover { opacity: .88; }

.res-field { margin-bottom: 16px; }
.res-field label { display: block; font-weight: 600; font-size: 13px; color: var(--text); margin-bottom: 5px; }
.res-field input { width: 100%; padding: 11px 13px; border: 1.5px solid var(--border); border-radius: 8px; font-size: 14px; background: #fafbff; font-family: inherit; }
.res-field input:focus { outline: none; border-color: var(--sky); }
.res-err { color: #c62828; font-size: 12px; margin-top: 3px; display: none; }
.res-note { font-size: 13px; color: var(--muted); line-height: 1.65; margin-bottom: 20px; }

#gpsGuardModal .modal-box { max-width: 500px; }
.gps-steps { list-style: none; margin: 14px 0; }
.gps-steps li { display: flex; align-items: flex-start; gap: 13px; padding: 11px 13px; border-radius: 8px; margin-bottom: 8px; background: #f8faff; border: 1px solid #e2eaf8; font-size: 13px; color: var(--text); }
.gps-steps li.done   { background: #f0fdf4; border-color: #bbf7d0; }
.gps-steps li.active { background: #fffbeb; border-color: #fde68a; }
.gps-steps li.fail   { background: #fff1f2; border-color: #fecdd3; }
.gps-steps li.skipped { background: #f0fdf4; border-color: #bbf7d0; opacity: .65; }
.step-icon { font-size: 18px; flex-shrink: 0; margin-top: 1px; }
.step-text strong { display: block; font-size: 13px; font-weight: 600; margin-bottom: 1px; }
.step-text span   { font-size: 12px; color: var(--muted); }
.gps-prog { height: 4px; background: #e5e7eb; border-radius: 4px; overflow: hidden; margin: 14px 0 0; }
.gps-prog-bar { height: 100%; background: linear-gradient(90deg, var(--blue), var(--green)); border-radius: 4px; transition: width .4s; width: 0; }

.first-survey-note { background: #eff6ff; border: 1px solid #bfdbfe; border-radius: 8px; padding: 11px 14px; margin-bottom: 14px; font-size: 13px; color: #1e40af; display: none; }
.first-survey-note strong { display: block; margin-bottom: 3px; }

.live-gps-card { background: #f0fdf4; border: 1px solid #bbf7d0; border-radius: 8px; padding: 12px 14px; margin-top: 14px; font-size: 12.5px; }
.live-gps-card strong { color: var(--navy); display: block; margin-bottom: 6px; font-size: 13px; }
.gps-coords { font-family: monospace; font-size: 12px; color: #166534; margin: 2px 0; }

#offlineModal .modal-box { max-width: 500px; }
.q-list { max-height: 240px; overflow-y: auto; margin: 12px 0; }
.q-item { display: flex; align-items: center; justify-content: space-between; padding: 11px 13px; border-radius: 8px; margin-bottom: 8px; background: #fef3c7; border: 1px solid #fde68a; font-size: 13px; }
.q-item .qi-info { color: #78350f; }
.q-item .qi-time { font-size: 11px; color: #92400e; margin-top: 2px; }
.q-item .qi-del  { font-size: 18px; cursor: pointer; color: var(--red); padding: 0 4px; border: none; background: none; }
.sync-prog-wrap { margin: 12px 0; display: none; }
.sync-prog-outer { height: 6px; background: #e5e7eb; border-radius: 6px; overflow: hidden; }
.sync-prog-inner { height: 100%; background: linear-gradient(90deg, var(--blue), var(--green)); border-radius: 6px; transition: width .4s; width: 0; }
.sync-status { font-size: 12px; color: var(--muted); margin-top: 6px; text-align: center; }

#declinedMsg { display: none; text-align: center; padding: 70px 20px; }
#gpsDistanceChip { display: none; margin-left: auto; font-size: 11.5px; font-weight: 700; padding: 3px 11px; border-radius: 12px; }

/* Conditional follow-up field */
.conditional-field { display: none; margin-top: 10px; }
.conditional-field.visible { display: block; }
</style>
</head>
<body>

<!-- CONSENT MODAL -->
<div class="modal-overlay" id="consentModal">
  <div class="modal-box">
    <div class="modal-head">
      <h2>📋 Informed Consent</h2>
      <p>Embu County Political &amp; Service Delivery Survey · ECS-2025 · Please read carefully</p>
    </div>
    <div class="modal-scroll">
      <div class="consent-section">
        <div class="consent-h4">1. Purpose of This Research</div>
        <p>You are invited to participate in a research study examining political participation, governance, service delivery, and development priorities in Embu County. Findings will inform policy discussions and community development planning.</p>
        <div class="consent-h4">2. What Participation Involves</div>
        <p>Your participation involves completing a questionnaire covering:</p>
        <ul>
          <li>Your demographic background (age, gender, location, education, employment)</li>
          <li>Political participation and civic engagement</li>
          <li>Opinions on local government transparency and accountability</li>
          <li>Assessment of public service delivery (health, education, agriculture, infrastructure)</li>
          <li>Economic challenges and development priorities in your ward</li>
          <li>Sentiment and preferences regarding county leadership</li>
        </ul>
        <div class="consent-h4">3. Duration</div>
        <p>Completing this questionnaire takes approximately <strong>20–35 minutes</strong>.</p>
        <div class="consent-h4">4. Voluntary Participation</div>
        <p>Participation is entirely voluntary. You may decline any question and may withdraw at any time without negative consequences.</p>
        <div class="consent-h4">5. Confidentiality &amp; Data Protection</div>
        <ul>
          <li>All information is kept strictly confidential.</li>
          <li>Responses will be anonymised and cannot be traced to you individually.</li>
          <li>Data is stored securely, accessible only to authorised research personnel.</li>
          <li>Aggregated findings may be published; no individual data will be disclosed.</li>
          <li>Contact information (if provided) is stored separately from survey responses.</li>
          <li>Your data will be handled per applicable data protection laws.</li>
          <li><strong>GPS coordinates</strong> are collected only to verify geographic coverage and prevent duplicate submissions — not to identify individuals.</li>
        </ul>
        <div class="consent-h4">6. Risks &amp; Benefits</div>
        <p>There are no known risks. Your contribution ensures community voices are represented in Embu County governance and policy-making.</p>
      </div>
    </div>
    <div class="modal-foot">
      <div class="consent-check">
        <input type="checkbox" id="consentCheck" onchange="toggleProceed()">
        <label for="consentCheck">I have read and understood the above. I voluntarily agree to participate and consent to data collection including GPS location for quality assurance purposes.</label>
      </div>
      <div class="btn-row">
        <button class="btn-decline" onclick="declineConsent()">Decline</button>
        <button class="btn-proceed" id="proceedBtn" disabled onclick="proceedAfterConsent()">I Agree &amp; Continue →</button>
      </div>
    </div>
  </div>
</div>

<!-- RESEARCHER MODAL -->
<div class="modal-overlay hidden" id="researcherModal">
  <div class="modal-box" style="max-width:480px;">
    <div class="modal-head">
      <h2>👤 Researcher Registration</h2>
      <p>First-time setup — saved on this device</p>
    </div>
    <div class="modal-scroll">
      <p class="res-note">Enter your name and researcher ID. These are saved locally so you don't need to re-enter them, and are attached to each submission for quality assurance.</p>
      <div class="res-field">
        <label for="resName">Full Name *</label>
        <input type="text" id="resName" placeholder="e.g. Jane Muthoni Kariuki" autocomplete="off">
        <div class="res-err" id="resNameErr">Please enter your full name.</div>
      </div>
      <div class="res-field">
        <label for="resId">Researcher / Enumerator ID *</label>
        <input type="text" id="resId" placeholder="e.g. ECS-R-001" autocomplete="off">
        <div class="res-err" id="resIdErr">Please enter your researcher ID.</div>
      </div>
    </div>
    <div class="modal-foot">
      <div class="btn-row">
        <button class="btn-proceed" onclick="saveResearcher()">Save &amp; Start Survey →</button>
      </div>
    </div>
  </div>
</div>

<!-- GPS GUARD MODAL -->
<div class="modal-overlay hidden" id="gpsGuardModal">
  <div class="modal-box" style="max-width:480px;">
    <div class="modal-head">
      <h2>📍 Location Verification</h2>
      <p id="gpsGuardSubtitle">Capturing GPS location — please wait</p>
    </div>
    <div class="modal-scroll">
      <div class="first-survey-note" id="firstSurveyNote">
        <strong>📌 First survey of the day</strong>
        Your starting location is being recorded. From the next survey onwards, the system will check that you have moved to a new location before submitting.
      </div>
      <ul class="gps-steps">
        <li id="step-acquire" class="active">
          <span class="step-icon">🛰️</span>
          <div class="step-text"><strong>Acquiring GPS Signal</strong><span id="step-acquire-detail">Waiting for location…</span></div>
        </li>
        <li id="step-accuracy">
          <span class="step-icon">🎯</span>
          <div class="step-text"><strong>Accuracy Check</strong><span id="step-accuracy-detail">Requires ≤ 80 m accuracy</span></div>
        </li>
        <li id="step-distance">
          <span class="step-icon">📏</span>
          <div class="step-text"><strong>Distance Check</strong><span id="step-distance-detail">Must be 500 m from last survey</span></div>
        </li>
        <li id="step-time">
          <span class="step-icon">⏱️</span>
          <div class="step-text"><strong>Time Gap Check</strong><span id="step-time-detail">Checking time since last survey</span></div>
        </li>
      </ul>
      <div class="gps-prog"><div class="gps-prog-bar" id="gpsProgressBar"></div></div>
      <div class="live-gps-card" id="liveGpsCard" style="display:none;">
        <strong>📡 GPS Fix Acquired</strong>
        <div class="gps-coords" id="liveGpsCoordsDisplay"></div>
        <div style="font-size:12px;color:var(--muted);margin-top:4px;" id="liveGpsAccDisplay"></div>
      </div>
    </div>
    <div class="modal-foot">
      <div class="btn-row">
        <button class="btn-decline" id="gpsCancelBtn" onclick="cancelGpsCheck()">Cancel</button>
        <button class="btn-proceed" id="gpsRetryBtn" style="display:none;" onclick="retryGpsCheck()">🔄 Retry</button>
      </div>
    </div>
  </div>
</div>

<!-- OFFLINE QUEUE MODAL -->
<div class="modal-overlay hidden" id="offlineModal">
  <div class="modal-box" style="max-width:480px;">
    <div class="modal-head">
      <h2>📤 Pending Submissions</h2>
      <p>Surveys saved locally while offline</p>
    </div>
    <div class="modal-scroll">
      <p style="font-size:13px;color:var(--muted);margin-bottom:12px;">These surveys were completed while offline. Connect to the internet and tap <strong>Sync Now</strong> to upload them.</p>
      <div class="q-list" id="queueList"></div>
      <div class="sync-prog-wrap" id="syncProgressWrap">
        <div class="sync-prog-outer"><div class="sync-prog-inner" id="syncProgressBar"></div></div>
        <div class="sync-status" id="syncStatusText">Syncing…</div>
      </div>
    </div>
    <div class="modal-foot">
      <div class="btn-row">
        <button class="btn-decline" onclick="closeOfflineModal()">Close</button>
        <button class="btn-proceed" id="syncNowBtn" onclick="syncPendingNow()">📡 Sync Now</button>
      </div>
    </div>
  </div>
</div>

<div id="declinedMsg">
  <div style="font-size:52px;margin-bottom:16px;">🙏</div>
  <h2 style="color:var(--navy);font-family:'DM Serif Display',serif;">Thank you for your time</h2>
  <p style="color:var(--muted);font-size:14px;margin-top:8px;">You have chosen not to participate.<br>Refresh this page if you change your mind.</p>
</div>

<div class="site-header">
  <h1>Embu County Political Survey</h1>
  <p>Research Questionnaire &amp; Informed Consent — ECS-2025</p>
  <div class="header-badge">Confidential · Research Purposes Only</div>
  <div class="header-actions">
    <button class="header-btn" onclick="downloadQuestionnaire()">⬇️ Download Questionnaire (PDF)</button>
  </div>
</div>

<div class="status-strip" id="gpsStatusBar">
  <div class="status-strip-inner">
    <div class="gps-dot" id="gpsDot"></div>
    <span style="font-weight:600;color:var(--text);">GPS:</span>
    <span id="gpsStatusText" style="color:var(--muted);">Inactive</span>
    <span id="gpsDistanceChip"></span>
  </div>
</div>

<div id="proximityLockBanner">
  <div class="lock-inner">
    <span style="font-size:18px;">🔒</span>
    <span id="proximityLockMsg">Please move to a different location before your next respondent.</span>
  </div>
</div>

<div id="draftBar">
  <div class="draft-inner">
    <div class="draft-left">
      <span style="font-size:17px;">📝</span>
      <div>
        <div class="draft-text">Unsaved draft found</div>
        <div class="draft-sub" id="draftSubText">Saved on …</div>
      </div>
    </div>
    <div style="display:flex;gap:8px;">
      <button class="draft-btn restore" onclick="restoreDraft()">↩ Restore</button>
      <button class="draft-btn discard" onclick="discardDraft()">✕ Discard</button>
    </div>
  </div>
</div>

<div id="networkBadge"></div>
<div id="autosaveIndicator">💾 Draft saved</div>

<div id="surveyWrapper">
<div class="container">

  <div id="researcherBanner">
    <div>
      <strong>🔬 Researcher: <span id="bannerName"></span></strong>
      &nbsp;·&nbsp; ID: <span id="bannerId"></span>
    </div>
    <div style="display:flex;gap:8px;flex-wrap:wrap;align-items:center;">
      <button class="res-action-btn pending" id="pendingQueueBtn" onclick="openOfflineModal()" style="display:none;">
        📤 Pending <span id="pendingCountBadge">0</span>
      </button>
      <button class="res-action-btn" onclick="changeResearcher()">Change Researcher</button>
    </div>
  </div>

  <div id="pendingBanner">
    <strong>📤 You have <span id="pendingCountText">0</span> survey(s) queued for upload.</strong>
    <div style="font-size:12px;margin-top:3px;">Saved while offline. Connect to sync them.</div>
    <button class="sync-btn" onclick="openOfflineModal()">View &amp; Sync →</button>
  </div>

  <form id="surveyForm" autocomplete="off">

  <!-- SECTION A -->
  <div class="card">
    <div class="card-head"><div class="section-badge">A</div><h3>Section A — Respondent Demographics</h3></div>
    <div class="card-body">
      <div class="field">
        <label class="field-label"><span class="qn">A1.</span> Gender</label>
        <div class="pill-row">
          <div class="pill"><input type="radio" name="gender" id="gm" value="Male" required><label for="gm">Male</label></div>
          <div class="pill"><input type="radio" name="gender" id="gf" value="Female"><label for="gf">Female</label></div>
          <div class="pill"><input type="radio" name="gender" id="gp" value="Prefer not to say"><label for="gp">Prefer not to say</label></div>
        </div>
      </div>
      <div class="field">
        <label class="field-label"><span class="qn">A2.</span> Constituency</label>
        <select id="constituency" required onchange="updateWards()">
          <option value="">Select constituency</option>
          <option>Manyatta</option><option>Runyenjes</option>
          <option>Mbeere North</option><option>Mbeere South</option>
        </select>
      </div>
      <div class="field">
        <label class="field-label"><span class="qn">A3.</span> Ward</label>
        <select id="ward" required onchange="populateWard()">
          <option value="">— Select constituency first —</option>
        </select>
      </div>
      <div class="field">
        <label class="field-label"><span class="qn">A4.</span> Sub-Location / Village</label>
        <input type="text" id="subLocation" placeholder="Enter your sub-location or village name">
      </div>
      <div class="loc-box" id="locationBox" style="display:none;">
        <div class="loc-box-head">📍 Interview Location (GPS)</div>
        <div class="loc-row">
          <span>Latitude: <span class="loc-val" id="locLat">—</span></span>
          <span>Longitude: <span class="loc-val" id="locLng">—</span></span>
          <span>Accuracy: <span class="loc-acc" id="locAcc">—</span></span>
        </div>
        <div style="font-size:11.5px;color:var(--muted);margin-top:6px;">Location captured automatically — not linked to your identity.</div>
      </div>
      <!-- A5: Updated age groups -->
      <div class="field">
        <label class="field-label"><span class="qn">A5.</span> Age Category</label>
        <div class="pill-row">
          <div class="pill"><input type="radio" name="age" id="a1" value="18-35" required><label for="a1">18–35</label></div>
          <div class="pill"><input type="radio" name="age" id="a2" value="36-60"><label for="a2">36–60</label></div>
          <div class="pill"><input type="radio" name="age" id="a3" value="61+"><label for="a3">61+</label></div>
        </div>
      </div>
      <!-- A6: Updated education with Tertiary combining college + university -->
      <div class="field">
        <label class="field-label"><span class="qn">A6.</span> Highest Education Level</label>
        <div class="pill-row">
          <div class="pill"><input type="radio" name="education" id="enon" value="None" required><label for="enon">None</label></div>
          <div class="pill"><input type="radio" name="education" id="epri" value="Primary"><label for="epri">Primary</label></div>
          <div class="pill"><input type="radio" name="education" id="esec" value="Secondary"><label for="esec">Secondary</label></div>
          <div class="pill"><input type="radio" name="education" id="eter" value="Tertiary (College/University)"><label for="eter">Tertiary (College / University)</label></div>
        </div>
      </div>
      <div class="field">
        <label class="field-label"><span class="qn">A7.</span> Employment Status</label>
        <div class="pill-row">
          <div class="pill"><input type="radio" name="employment" id="ee" value="Employed" required><label for="ee">Employed</label></div>
          <div class="pill"><input type="radio" name="employment" id="ese" value="Self-Employed"><label for="ese">Self-Employed</label></div>
          <div class="pill"><input type="radio" name="employment" id="eu" value="Unemployed"><label for="eu">Unemployed</label></div>
        </div>
      </div>
    </div>
  </div>

  <!-- SECTION B -->
  <div class="card">
    <div class="card-head"><div class="section-badge">B</div><h3>Section B — Political Participation &amp; Governance</h3></div>
    <div class="card-body">
      <!-- B1: Changed to "Are you a registered voter?" -->
      <div class="field">
        <label class="field-label"><span class="qn">B1.</span> Are you a registered voter?</label>
        <div class="pill-row">
          <div class="pill"><input type="radio" name="voter" id="vy" value="Yes" required><label for="vy">Yes</label></div>
          <div class="pill"><input type="radio" name="voter" id="vn" value="No"><label for="vn">No</label></div>
        </div>
      </div>
      <!-- B2: How often do you engage in politics? If Never, explain -->
      <div class="field">
        <label class="field-label"><span class="qn">B2.</span> How often do you engage in politics?</label>
        <div class="pill-row">
          <div class="pill"><input type="radio" name="engage" id="ef" value="Frequently" required onchange="toggleEngageExplain()"><label for="ef">Frequently</label></div>
          <div class="pill"><input type="radio" name="engage" id="eo2" value="Occasionally" onchange="toggleEngageExplain()"><label for="eo2">Occasionally</label></div>
          <div class="pill"><input type="radio" name="engage" id="er" value="Rarely" onchange="toggleEngageExplain()"><label for="er">Rarely</label></div>
          <div class="pill"><input type="radio" name="engage" id="en" value="Never" onchange="toggleEngageExplain()"><label for="en">Never</label></div>
        </div>
        <div class="conditional-field" id="engageNeverExplain">
          <label class="field-label" style="margin-top:8px;"><span class="qn">B2.1.</span> If Never — please explain why:</label>
          <textarea id="engageNeverReason" placeholder="Please explain why you do not engage in politics…"></textarea>
        </div>
      </div>
      <!-- B2.2: What motivates your participation? -->
      <div class="field">
        <label class="field-label"><span class="qn">B2.2.</span> What motivates your participation in politics? <span class="field-note">(tick all that apply)</span></label>
        <div class="pill-row">
          <div class="pill"><input type="checkbox" id="b3a" value="Community development"><label for="b3a">Community development</label></div>
          <div class="pill"><input type="checkbox" id="b3b" value="Personal interests"><label for="b3b">Personal interests</label></div>
          <div class="pill"><input type="checkbox" id="b3c" value="Civic duty"><label for="b3c">Civic duty</label></div>
          <div class="pill"><input type="checkbox" id="b3d" value="Other"><label for="b3d">Other</label></div>
        </div>
      </div>
      <!-- B3: Do you feel your opinions are considered in governance? -->
      <div class="field">
        <label class="field-label"><span class="qn">B3.</span> Do you feel your opinions are considered in governance?</label>
        <div class="pill-row">
          <div class="pill"><input type="radio" name="opinion" id="opy" value="Yes" required><label for="opy">Yes</label></div>
          <div class="pill"><input type="radio" name="opinion" id="opn" value="No"><label for="opn">No</label></div>
          <div class="pill"><input type="radio" name="opinion" id="opns" value="Not sure"><label for="opns">Not sure</label></div>
        </div>
      </div>
      <div class="field">
        <label class="field-label"><span class="qn">B4.</span> What are the most pressing political issues in the county?</label>
        <textarea id="issues" placeholder="Describe the key political issues…"></textarea>
      </div>
      <!-- B5 (was B6): Areas where the county government has NOT delivered -->
      <div class="field">
        <label class="field-label"><span class="qn">B5.</span> Areas where the county government has NOT delivered: <span class="field-note">(tick all that apply)</span></label>
        <div class="pill-row">
          <div class="pill"><input type="checkbox" id="b6a" value="Agriculture"><label for="b6a">Agriculture</label></div>
          <div class="pill"><input type="checkbox" id="b6b" value="Infrastructure"><label for="b6b">Infrastructure</label></div>
          <div class="pill"><input type="checkbox" id="b6c" value="Investments"><label for="b6c">Investments</label></div>
          <div class="pill"><input type="checkbox" id="b6d" value="SMEs"><label for="b6d">SMEs</label></div>
          <div class="pill"><input type="checkbox" id="b6e" value="Health"><label for="b6e">Health</label></div>
          <div class="pill"><input type="checkbox" id="b6f" value="Education"><label for="b6f">Education</label></div>
        </div>
      </div>
    </div>
  </div>

  <!-- SECTION C -->
  <div class="card">
    <div class="card-head"><div class="section-badge">C</div><h3>Section C — Political Information &amp; Transparency</h3></div>
    <div class="card-body">
      <div class="field">
        <label class="field-label"><span class="qn">C1.</span> How do you usually get political information? <span class="field-note">(tick all that apply)</span></label>
        <div class="pill-row">
          <div class="pill"><input type="checkbox" id="c1a" value="Radio"><label for="c1a">Radio</label></div>
          <div class="pill"><input type="checkbox" id="c1b" value="TV"><label for="c1b">TV</label></div>
          <div class="pill"><input type="checkbox" id="c1c" value="Newspapers"><label for="c1c">Newspapers</label></div>
          <div class="pill"><input type="checkbox" id="c1d" value="Social Media"><label for="c1d">Social Media</label></div>
          <div class="pill"><input type="checkbox" id="c1e" value="Word of Mouth"><label for="c1e">Word of Mouth</label></div>
          <div class="pill"><input type="checkbox" id="c1f" value="Barazas"><label for="c1f">Barazas</label></div>
          <div class="pill"><input type="checkbox" id="c1g" value="Other"><label for="c1g">Other</label></div>
        </div>
      </div>
      <!-- C2: Rate transparency of government (removed "local") -->
      <div class="field">
        <label class="field-label"><span class="qn">C2.</span> Rate the transparency of the government:</label>
        <div class="pill-row">
          <div class="pill"><input type="radio" name="transparency" id="tex" value="Excellent" required><label for="tex">Excellent</label></div>
          <div class="pill"><input type="radio" name="transparency" id="tgd" value="Good"><label for="tgd">Good</label></div>
          <div class="pill"><input type="radio" name="transparency" id="tfa" value="Fair"><label for="tfa">Fair</label></div>
          <div class="pill"><input type="radio" name="transparency" id="tpo" value="Poor"><label for="tpo">Poor</label></div>
        </div>
      </div>
    </div>
  </div>

  <!-- SECTION D -->
  <div class="card">
    <div class="card-head"><div class="section-badge">D</div><h3>Section D — Project Delivery Assessment</h3></div>
    <div class="card-body">
      <div class="field">
        <label class="field-label"><span class="qn">D1.</span> Rate the performance of the current county government:</label>
        <div class="pill-row">
          <div class="pill"><input type="radio" name="performance" id="pex" value="Excellent" required><label for="pex">Excellent</label></div>
          <div class="pill"><input type="radio" name="performance" id="pgd" value="Good"><label for="pgd">Good</label></div>
          <div class="pill"><input type="radio" name="performance" id="pfa" value="Fair"><label for="pfa">Fair</label></div>
          <div class="pill"><input type="radio" name="performance" id="ppo" value="Poor"><label for="ppo">Poor</label></div>
        </div>
      </div>
      <!-- D2: Updated question -->
      <div class="field">
        <label class="field-label"><span class="qn">D2.</span> Are you aware of any projects delivered by the county government in your area? If yes, name up to 2:</label>
        <div class="pill-row" style="margin-bottom:8px;">
          <div class="pill"><input type="radio" name="projectsAware" id="paw_y" value="Yes" onchange="toggleProjectsDelivered()"><label for="paw_y">Yes</label></div>
          <div class="pill"><input type="radio" name="projectsAware" id="paw_n" value="No" onchange="toggleProjectsDelivered()"><label for="paw_n">No</label></div>
        </div>
        <div class="conditional-field" id="projectsDeliveredField">
          <textarea id="projectsDelivered" placeholder="1. &#10;2. "></textarea>
        </div>
      </div>
      <!-- D3 (was D5): Promised projects not delivered -->
      <div class="field">
        <label class="field-label"><span class="qn">D3.</span> Are there any projects that were promised but have not been delivered? If yes, name up to 2:</label>
        <div class="pill-row" style="margin-bottom:8px;">
          <div class="pill"><input type="radio" name="missedPromises" id="mp_y" value="Yes" onchange="toggleProjectsMissing()"><label for="mp_y">Yes</label></div>
          <div class="pill"><input type="radio" name="missedPromises" id="mp_n" value="No" onchange="toggleProjectsMissing()"><label for="mp_n">No</label></div>
        </div>
        <div class="conditional-field" id="projectsMissingField">
          <textarea id="projectsMissing" placeholder="1. &#10;2. "></textarea>
        </div>
      </div>
      <div class="field">
        <label class="field-label"><span class="qn">D4.</span> List your top 5 priority needs in your ward (in order of priority):</label>
        <textarea id="needs" placeholder="1. &#10;2. &#10;3. &#10;4. &#10;5. " style="min-height:110px;"></textarea>
      </div>
    </div>
  </div>

  <!-- SECTION E -->
  <div class="card">
    <div class="card-head"><div class="section-badge">E</div><h3>Section E — Service Delivery Rating</h3></div>
    <div class="card-body">
      <!-- E1: NEW question -->
      <div class="field">
        <label class="field-label"><span class="qn">E1.</span> How often do you pay attention to development activities in Embu County?</label>
        <div class="pill-row">
          <div class="pill"><input type="radio" name="devAttention" id="da1" value="Always" required><label for="da1">Always</label></div>
          <div class="pill"><input type="radio" name="devAttention" id="da2" value="Often"><label for="da2">Often</label></div>
          <div class="pill"><input type="radio" name="devAttention" id="da3" value="Sometimes"><label for="da3">Sometimes</label></div>
          <div class="pill"><input type="radio" name="devAttention" id="da4" value="Rarely"><label for="da4">Rarely</label></div>
          <div class="pill"><input type="radio" name="devAttention" id="da5" value="Never"><label for="da5">Never</label></div>
        </div>
      </div>
      <!-- E2: Service table with expanded rows -->
      <div class="field">
        <label class="field-label"><span class="qn">E2.</span> Rate how the county government has performed in the following areas:</label>
        <p class="section-note">Not Sure / Very Poor / Poor / Fair / Good / Excellent</p>
        <div style="overflow-x:auto;">
          <table class="svc-table">
            <thead>
              <tr>
                <th style="text-align:left;min-width:180px;">Service Area</th>
                <th>Not Sure</th><th>Very Poor</th><th>Poor</th><th>Fair</th><th>Good</th><th>Excellent</th>
              </tr>
            </thead>
            <tbody>
              <tr><td>Agriculture Services</td><td><input type="radio" name="svc_agri" value="Not Sure"></td><td><input type="radio" name="svc_agri" value="Very Poor"></td><td><input type="radio" name="svc_agri" value="Poor"></td><td><input type="radio" name="svc_agri" value="Fair"></td><td><input type="radio" name="svc_agri" value="Good"></td><td><input type="radio" name="svc_agri" value="Excellent"></td></tr>
              <tr><td>Infrastructure (Roads &amp; Markets)</td><td><input type="radio" name="svc_infra" value="Not Sure"></td><td><input type="radio" name="svc_infra" value="Very Poor"></td><td><input type="radio" name="svc_infra" value="Poor"></td><td><input type="radio" name="svc_infra" value="Fair"></td><td><input type="radio" name="svc_infra" value="Good"></td><td><input type="radio" name="svc_infra" value="Excellent"></td></tr>
              <tr><td>Education — ECD / ECDE</td><td><input type="radio" name="svc_ecd" value="Not Sure"></td><td><input type="radio" name="svc_ecd" value="Very Poor"></td><td><input type="radio" name="svc_ecd" value="Poor"></td><td><input type="radio" name="svc_ecd" value="Fair"></td><td><input type="radio" name="svc_ecd" value="Good"></td><td><input type="radio" name="svc_ecd" value="Excellent"></td></tr>
              <tr><td>Education — VTCs</td><td><input type="radio" name="svc_vtc" value="Not Sure"></td><td><input type="radio" name="svc_vtc" value="Very Poor"></td><td><input type="radio" name="svc_vtc" value="Poor"></td><td><input type="radio" name="svc_vtc" value="Fair"></td><td><input type="radio" name="svc_vtc" value="Good"></td><td><input type="radio" name="svc_vtc" value="Excellent"></td></tr>
              <tr><td>Health Services</td><td><input type="radio" name="svc_health" value="Not Sure"></td><td><input type="radio" name="svc_health" value="Very Poor"></td><td><input type="radio" name="svc_health" value="Poor"></td><td><input type="radio" name="svc_health" value="Fair"></td><td><input type="radio" name="svc_health" value="Good"></td><td><input type="radio" name="svc_health" value="Excellent"></td></tr>
              <tr><td>Youth Empowerment (SIGPs)</td><td><input type="radio" name="svc_youth" value="Not Sure"></td><td><input type="radio" name="svc_youth" value="Very Poor"></td><td><input type="radio" name="svc_youth" value="Poor"></td><td><input type="radio" name="svc_youth" value="Fair"></td><td><input type="radio" name="svc_youth" value="Good"></td><td><input type="radio" name="svc_youth" value="Excellent"></td></tr>
              <tr><td>Women Empowerment (SIGPs)</td><td><input type="radio" name="svc_women" value="Not Sure"></td><td><input type="radio" name="svc_women" value="Very Poor"></td><td><input type="radio" name="svc_women" value="Poor"></td><td><input type="radio" name="svc_women" value="Fair"></td><td><input type="radio" name="svc_women" value="Good"></td><td><input type="radio" name="svc_women" value="Excellent"></td></tr>
              <tr><td>PWDs Support (SIGPs)</td><td><input type="radio" name="svc_pwds" value="Not Sure"></td><td><input type="radio" name="svc_pwds" value="Very Poor"></td><td><input type="radio" name="svc_pwds" value="Poor"></td><td><input type="radio" name="svc_pwds" value="Fair"></td><td><input type="radio" name="svc_pwds" value="Good"></td><td><input type="radio" name="svc_pwds" value="Excellent"></td></tr>
              <tr><td>Access to Gov't Services</td><td><input type="radio" name="svc_govtaccess" value="Not Sure"></td><td><input type="radio" name="svc_govtaccess" value="Very Poor"></td><td><input type="radio" name="svc_govtaccess" value="Poor"></td><td><input type="radio" name="svc_govtaccess" value="Fair"></td><td><input type="radio" name="svc_govtaccess" value="Good"></td><td><input type="radio" name="svc_govtaccess" value="Excellent"></td></tr>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

  <!-- SECTION F: Slimmed down to F1 only (SME challenges) -->
  <div class="card">
    <div class="card-head"><div class="section-badge">F</div><h3>Section F — Business &amp; Economic Issues</h3></div>
    <div class="card-body">
      <div class="field">
        <label class="field-label"><span class="qn">F1.</span> What are the main challenges faced by business people / SMEs in Embu County? <span class="field-note">(tick all that apply)</span></label>
        <div class="pill-row">
          <div class="pill"><input type="checkbox" id="f4a" value="Finance / Capital"><label for="f4a">Finance / Capital</label></div>
          <div class="pill"><input type="checkbox" id="f4b" value="High Taxes"><label for="f4b">High Taxes</label></div>
          <div class="pill"><input type="checkbox" id="f4c" value="Limited Market"><label for="f4c">Limited Market</label></div>
          <div class="pill"><input type="checkbox" id="f4d" value="Lack of Government Support"><label for="f4d">Lack of Government Support</label></div>
          <div class="pill"><input type="checkbox" id="f4e" value="Competition"><label for="f4e">Competition</label></div>
          <div class="pill"><input type="checkbox" id="f4f" value="Poor Infrastructure"><label for="f4f">Poor Infrastructure</label></div>
          <div class="pill"><input type="checkbox" id="f4g" value="Other"><label for="f4g">Other</label></div>
        </div>
      </div>
    </div>
  </div>

  <!-- SECTION G -->
  <div class="card">
    <div class="card-head"><div class="section-badge">G</div><h3>Section G — Political Sentiment</h3></div>
    <div class="card-body">
      <div class="field">
        <label class="field-label"><span class="qn">G1.</span> If an election were held today, who would you vote for as Governor?</label>
        <div class="gov-grid" id="governorGrid">
          <div class="gov-card" onclick="selectGovernor(this,'cecily')"><input type="radio" name="governor" value="Cecily Mbarire" onchange="updateNegativeQuestions()"><div class="gc-name">Cecily Mbarire</div><div class="gc-tag">Incumbent Governor</div></div>
          <div class="gov-card" onclick="selectGovernor(this,'lenny')"><input type="radio" name="governor" value="Lenny Kivuti" onchange="updateNegativeQuestions()"><div class="gc-name">Lenny Kivuti</div><div class="gc-tag">Challenger</div></div>
          <div class="gov-card" onclick="selectGovernor(this,'new')"><input type="radio" name="governor" value="A new face" onchange="updateNegativeQuestions()"><div class="gc-name">A New Face</div><div class="gc-tag">New candidate</div></div>
          <div class="gov-card" onclick="selectGovernor(this,'undecided')"><input type="radio" name="governor" value="Not yet decided" onchange="updateNegativeQuestions()"><div class="gc-name">Not Yet Decided</div><div class="gc-tag">Undecided</div></div>
        </div>
      </div>
      <!-- G2: Political party affiliation -->
      <div class="field">
        <label class="field-label"><span class="qn">G2.</span> Which political party do you identify with or support?</label>
        <div class="pill-row">
          <div class="pill"><input type="radio" name="politicalParty" id="party_bdep" value="BDEP"><label for="party_bdep">BDEP</label></div>
          <div class="pill"><input type="radio" name="politicalParty" id="party_uda" value="UDA"><label for="party_uda">UDA</label></div>
          <div class="pill"><input type="radio" name="politicalParty" id="party_jub" value="Jubilee"><label for="party_jub">Jubilee</label></div>
          <div class="pill"><input type="radio" name="politicalParty" id="party_dp" value="DP"><label for="party_dp">DP</label></div>
          <div class="pill"><input type="radio" name="politicalParty" id="party_dcp" value="DCP"><label for="party_dcp">DCP</label></div>
          <div class="pill"><input type="radio" name="politicalParty" id="party_other" value="Other"><label for="party_other">Other</label></div>
          <div class="pill"><input type="radio" name="politicalParty" id="party_none" value="None"><label for="party_none">None</label></div>
        </div>
      </div>
      <div class="field">
        <label class="field-label"><span class="qn">G3.</span> Why would you vote for that person? <span class="field-note">(tick all that apply)</span></label>
        <div class="pill-row">
          <div class="pill"><input type="checkbox" id="g2a" value="Track record"><label for="g2a">Track record</label></div>
          <div class="pill"><input type="checkbox" id="g2b" value="Development agenda"><label for="g2b">Development agenda</label></div>
          <div class="pill"><input type="checkbox" id="g2c" value="Trustworthy"><label for="g2c">Trustworthy</label></div>
          <div class="pill"><input type="checkbox" id="g2d" value="Leadership qualities"><label for="g2d">Leadership qualities</label></div>
          <div class="pill"><input type="checkbox" id="g2e" value="Community connection"><label for="g2e">Community connection</label></div>
          <div class="pill"><input type="checkbox" id="g2f" value="Communication"><label for="g2f">Communication</label></div>
          <div class="pill"><input type="checkbox" id="g2g" value="Proven delivery"><label for="g2g">Proven delivery</label></div>
          <div class="pill"><input type="checkbox" id="g2h" value="Party affiliation"><label for="g2h">Party affiliation</label></div>
          <div class="pill"><input type="checkbox" id="g2i" value="New ideas"><label for="g2i">New ideas</label></div>
          <div class="pill"><input type="checkbox" id="g2j" value="Personal help"><label for="g2j">Personal help</label></div>
          <div class="pill"><input type="checkbox" id="g2k" value="Other"><label for="g2k">Other</label></div>
        </div>
      </div>
      <div class="field" id="notMbarireField">
        <label class="field-label"><span class="qn">G4.</span> Reasons for NOT voting for Cecily Mbarire: <span class="field-note">(tick all that apply)</span></label>
        <div class="pill-row" id="notMbarireOptions">
          <div class="pill"><input type="checkbox" id="g3a" value="Poor performance"><label for="g3a">Poor performance</label></div>
          <div class="pill"><input type="checkbox" id="g3b" value="Corruption"><label for="g3b">Corruption</label></div>
          <div class="pill"><input type="checkbox" id="g3c" value="Untrustworthy"><label for="g3c">Untrustworthy</label></div>
          <div class="pill"><input type="checkbox" id="g3d" value="Tribal favoritism"><label for="g3d">Tribal favoritism</label></div>
          <div class="pill"><input type="checkbox" id="g3e" value="Party affiliation"><label for="g3e">Party affiliation</label></div>
          <div class="pill"><input type="checkbox" id="g3f" value="Weak leadership"><label for="g3f">Weak leadership</label></div>
          <div class="pill"><input type="checkbox" id="g3g" value="No vision"><label for="g3g">No vision</label></div>
          <div class="pill"><input type="checkbox" id="g3h" value="Land issues"><label for="g3h">Land issues</label></div>
          <div class="pill"><input type="checkbox" id="g3i" value="Other"><label for="g3i">Other</label></div>
        </div>
        <div id="notMbarireDisabledMsg" style="display:none;background:#f5f5f5;padding:11px;border-radius:7px;color:var(--muted);font-size:13px;margin-top:8px;">⚠️ Disabled — you selected Cecily Mbarire as your preferred candidate.</div>
      </div>
      <div class="field"><label class="field-label"><span class="qn">G5.</span> Advice to Cecily Mbarire:</label><textarea id="adviceMbarire" placeholder="Your advice or message to Cecily Mbarire…"></textarea></div>
      <div class="field" id="notKivutiField">
        <label class="field-label"><span class="qn">G6.</span> Reasons for NOT voting for Lenny Kivuti: <span class="field-note">(tick all that apply)</span></label>
        <div class="pill-row" id="notKivutiOptions">
          <div class="pill"><input type="checkbox" id="g5a" value="Poor performance"><label for="g5a">Poor performance</label></div>
          <div class="pill"><input type="checkbox" id="g5b" value="Corruption"><label for="g5b">Corruption</label></div>
          <div class="pill"><input type="checkbox" id="g5c" value="Untrustworthy"><label for="g5c">Untrustworthy</label></div>
          <div class="pill"><input type="checkbox" id="g5d" value="Tribal favoritism"><label for="g5d">Tribal favoritism</label></div>
          <div class="pill"><input type="checkbox" id="g5e" value="Party affiliation"><label for="g5e">Party affiliation</label></div>
          <div class="pill"><input type="checkbox" id="g5f" value="Weak leadership"><label for="g5f">Weak leadership</label></div>
          <div class="pill"><input type="checkbox" id="g5g" value="No vision"><label for="g5g">No vision</label></div>
          <div class="pill"><input type="checkbox" id="g5h" value="Land issues"><label for="g5h">Land issues</label></div>
          <div class="pill"><input type="checkbox" id="g5i" value="Other"><label for="g5i">Other</label></div>
        </div>
        <div id="notKivutiDisabledMsg" style="display:none;background:#f5f5f5;padding:11px;border-radius:7px;color:var(--muted);font-size:13px;margin-top:8px;">⚠️ Disabled — you selected Lenny Kivuti as your preferred candidate.</div>
      </div>
      <div class="field"><label class="field-label"><span class="qn">G7.</span> Advice to Lenny Kivuti:</label><textarea id="adviceKivuti" placeholder="Your advice or message to Lenny Kivuti…"></textarea></div>
      <div class="field"><label class="field-label"><span class="qn">G8.</span> Additional comments about the political state of Embu County:</label><textarea id="comments" placeholder="Share any final thoughts…"></textarea></div>
    </div>
  </div>

  <!-- SECTION H: New questions added -->
  <div class="card">
    <div class="card-head"><div class="section-badge">H</div><h3>Section H — Civic &amp; Community Perspectives</h3></div>
    <div class="card-body">
      <!-- H1: Which office contributes most to development -->
      <div class="field">
        <label class="field-label"><span class="qn">H1.</span> Which office do you think contributes most to development in your area?</label>
        <div class="pill-row">
          <div class="pill"><input type="radio" name="devOffice" id="dev_pres" value="President" required><label for="dev_pres">President</label></div>
          <div class="pill"><input type="radio" name="devOffice" id="dev_gov" value="Governor"><label for="dev_gov">Governor</label></div>
          <div class="pill"><input type="radio" name="devOffice" id="dev_mp" value="MP"><label for="dev_mp">MP</label></div>
          <div class="pill"><input type="radio" name="devOffice" id="dev_mca" value="MCA"><label for="dev_mca">MCA</label></div>
          <div class="pill"><input type="radio" name="devOffice" id="dev_sen" value="Senator"><label for="dev_sen">Senator</label></div>
          <div class="pill"><input type="radio" name="devOffice" id="dev_wr" value="Women Rep"><label for="dev_wr">Women Rep</label></div>
        </div>
      </div>
      <!-- H2: How often do you attend places of worship -->
      <div class="field">
        <label class="field-label"><span class="qn">H2.</span> How often do you attend places of worship (church / mosque)?</label>
        <div class="pill-row">
          <div class="pill"><input type="radio" name="worship" id="ws_reg" value="Regularly" required><label for="ws_reg">Regularly</label></div>
          <div class="pill"><input type="radio" name="worship" id="ws_rar" value="Rarely"><label for="ws_rar">Rarely</label></div>
          <div class="pill"><input type="radio" name="worship" id="ws_nev" value="Never"><label for="ws_nev">Never</label></div>
        </div>
      </div>
    </div>
  </div>

  <!-- SECTION I: Optional Contact Details -->
  <div class="card">
    <div class="card-head green"><div class="section-badge">I</div><h3>Section I — Optional Contact Details <span class="opt-badge" style="background:rgba(255,255,255,.25);color:#fff;">Optional</span></h3></div>
    <div class="card-body">
      <p class="section-note">Strictly optional. Contact details are stored separately from your survey responses and used only for research follow-up — never shared with third parties.</p>
      <div class="two-col">
        <div class="field"><label class="field-label">Full Name <span class="opt-badge">optional</span></label><input type="text" id="contactName" placeholder="Your full name"></div>
        <div class="field"><label class="field-label">Phone Number <span class="opt-badge">optional</span></label><input type="text" id="contactPhone" placeholder="e.g. 07XX XXX XXX"></div>
      </div>
      <div class="field">
        <label class="field-label">Consent to be contacted for follow-up?</label>
        <div class="pill-row">
          <div class="pill"><input type="radio" name="followUp" id="fuy" value="Yes"><label for="fuy">Yes</label></div>
          <div class="pill"><input type="radio" name="followUp" id="fun" value="No"><label for="fun">No</label></div>
        </div>
      </div>
    </div>
  </div>

  <div class="submit-wrap">
    <button type="submit" id="submitBtn">📍 Submit Survey</button>
    <div id="status" class="status-msg" style="display:none;"></div>
  </div>

  </form>
</div>
</div>

<script>
/* ══ FIREBASE ══ */
const firebaseConfig = {
  apiKey: "AIzaSyAXeNgPLKFgfxBKNta7UlGzHW-bjWzghas",
  authDomain: "survey-842bd.firebaseapp.com",
  projectId: "survey-842bd",
  storageBucket: "survey-842bd.firebasestorage.app",
  messagingSenderId: "916255795370",
  appId: "1:916255795370:web:3f186720bf9e2b163dc8d3",
  measurementId: "G-JZLGDGS974"
};
firebase.initializeApp(firebaseConfig);
const db = firebase.firestore();

/* ══ GPS CONFIG ══ */
const GPS_CONFIG = {
  MAX_ACCURACY_METERS  : 80,
  MIN_DISTANCE_METERS  : 500,
  MIN_TIME_GAP_MS      : 120000,
  HIGH_ACC_TIMEOUT_MS  : 25000,
  LOW_ACC_TIMEOUT_MS   : 20000,
  MAX_AGE_MS           : 15000
};

/* ══ WARD DATA ══ */
const WARDS = {
  'Manyatta':     ['Ruguru/Ngandori','Kithimu','Nginda','Mbeti North','Kirimari','Gaturi South'],
  'Runyenjes':    ['Gaturi North','Kagaari South','Kagaari North','Central Ward','Kyeni North','Kyeni South'],
  'Mbeere South': ['Mwea','Makima','Mbeti South','Mavuria','Kiambere'],
  'Mbeere North': ['Nthawa','Muminji','Evurore']
};

function updateWards(keepVal) {
  const c = document.getElementById('constituency').value;
  const ws = document.getElementById('ward');
  const prev = keepVal ? ws.value : '';
  ws.innerHTML = '';
  if (!c || !WARDS[c]) { ws.innerHTML = '<option value="">— Select constituency first —</option>'; return; }
  ws.appendChild(Object.assign(document.createElement('option'), { value:'', textContent:'Select ward' }));
  WARDS[c].forEach(w => ws.appendChild(Object.assign(document.createElement('option'), { value:w, textContent:w })));
  if (prev) ws.value = prev;
}
function populateWard() {}

/* ══ CONDITIONAL FIELDS ══ */
function toggleEngageExplain() {
  const sel = document.querySelector('input[name="engage"]:checked');
  const box = document.getElementById('engageNeverExplain');
  if (sel && sel.value === 'Never') {
    box.classList.add('visible');
  } else {
    box.classList.remove('visible');
    document.getElementById('engageNeverReason').value = '';
  }
}
function toggleProjectsDelivered() {
  const sel = document.querySelector('input[name="projectsAware"]:checked');
  document.getElementById('projectsDeliveredField').classList.toggle('visible', sel && sel.value === 'Yes');
}
function toggleProjectsMissing() {
  const sel = document.querySelector('input[name="missedPromises"]:checked');
  document.getElementById('projectsMissingField').classList.toggle('visible', sel && sel.value === 'Yes');
}

/* ══ HELPERS ══ */
function getTodayKey() { return new Date().toISOString().slice(0,10); }
function getResRecord() {
  try { const s = JSON.parse(localStorage.getItem('ecs_gps_last') || 'null'); return (s && s.day === getTodayKey()) ? s : null; } catch(e) { return null; }
}
function saveResRecord(lat, lng, ts) { localStorage.setItem('ecs_gps_last', JSON.stringify({ lat, lng, ts, day: getTodayKey() })); }
function haversineM(lat1, lng1, lat2, lng2) {
  const R = 6371000, dLat = (lat2-lat1)*Math.PI/180, dLng = (lng2-lng1)*Math.PI/180;
  const a = Math.sin(dLat/2)**2 + Math.cos(lat1*Math.PI/180)*Math.cos(lat2*Math.PI/180)*Math.sin(dLng/2)**2;
  return R * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a));
}
function getChecked(ids) { return ids.filter(id => { const e = document.getElementById(id); return e && e.checked; }).map(id => document.getElementById(id).value); }

/* ══ DRAFT / AUTOSAVE ══ */
const DRAFT_KEY = 'ecs_form_draft', QUEUE_KEY = 'ecs_offline_queue';
let autosaveTimer = null;

function collectSnap() {
  const s = {};
  document.querySelectorAll('input[type="radio"]').forEach(r => { if(r.checked) s[r.name] = r.value; });
  document.querySelectorAll('input[type="checkbox"]').forEach(cb => { s['cb_'+cb.id] = cb.checked; });
  document.querySelectorAll('input[type="text"],textarea,select').forEach(el => { s[el.id] = el.value; });
  s._savedAt = new Date().toISOString(); return s;
}
function applySnap(s) {
  if(!s) return;
  document.querySelectorAll('input[type="radio"]').forEach(r => { if(s[r.name]!==undefined) r.checked=(r.value===s[r.name]); });
  document.querySelectorAll('input[type="checkbox"]').forEach(cb => { if(s['cb_'+cb.id]!==undefined) cb.checked=s['cb_'+cb.id]; });
  document.querySelectorAll('input[type="text"],textarea,select').forEach(el => { if(s[el.id]!==undefined) el.value=s[el.id]; });
  updateWards(true);
  const gv = s['governor'];
  if(gv) document.querySelectorAll('.gov-card').forEach(c => { const r = c.querySelector('input[type="radio"]'); if(r&&r.value===gv) c.classList.add('selected'); });
  updateNegativeQuestions();
  toggleEngageExplain();
  toggleProjectsDelivered();
  toggleProjectsMissing();
}
function saveDraft() { localStorage.setItem(DRAFT_KEY, JSON.stringify(collectSnap())); showAutoSave(); }
function clearDraft() { localStorage.removeItem(DRAFT_KEY); }
function loadDraft() { try { return JSON.parse(localStorage.getItem(DRAFT_KEY)||'null'); } catch(e) { return null; } }
function showAutoSave() {
  const el = document.getElementById('autosaveIndicator');
  el.style.display = 'block'; el.style.opacity = '1';
  clearTimeout(el._t);
  el._t = setTimeout(() => { el.style.opacity = '0'; setTimeout(() => el.style.display = 'none', 400); }, 2200);
}
function checkForDraft() {
  const d = loadDraft(); if(!d) return;
  const dt = new Date(d._savedAt);
  document.getElementById('draftSubText').textContent = 'Last saved: '+dt.toLocaleDateString()+' '+dt.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'});
  document.getElementById('draftBar').style.display = 'block';
}
function restoreDraft() { applySnap(loadDraft()); document.getElementById('draftBar').style.display = 'none'; showAutoSave(); }
function discardDraft() {
  if(!confirm('Discard the saved draft and start fresh?')) return;
  clearDraft(); document.getElementById('draftBar').style.display = 'none';
}
function attachAutosave() {
  const f = document.getElementById('surveyForm');
  f.addEventListener('input',  () => { clearTimeout(autosaveTimer); autosaveTimer = setTimeout(saveDraft, 1500); });
  f.addEventListener('change', () => { clearTimeout(autosaveTimer); autosaveTimer = setTimeout(saveDraft, 1500); });
}

/* ══ OFFLINE QUEUE ══ */
function getQueue() { try { return JSON.parse(localStorage.getItem(QUEUE_KEY)||'[]'); } catch(e) { return []; } }
function saveQueue(q) { localStorage.setItem(QUEUE_KEY, JSON.stringify(q)); }
function enqueue(d) { d._queuedAt = new Date().toISOString(); d._queueId = 'q_'+Date.now()+'_'+Math.random().toString(36).slice(2,7); const q = getQueue(); q.push(d); saveQueue(q); refreshPendingUI(); }
function dequeue(id) { saveQueue(getQueue().filter(d => d._queueId !== id)); refreshPendingUI(); }
function refreshPendingUI() {
  const n = getQueue().length;
  document.getElementById('pendingQueueBtn').style.display = n > 0 ? 'inline-block' : 'none';
  document.getElementById('pendingBanner').style.display   = n > 0 ? 'block'        : 'none';
  const ct = document.getElementById('pendingCountText');  if(ct) ct.textContent = n;
  const cb = document.getElementById('pendingCountBadge'); if(cb) cb.textContent = n;
}
function openOfflineModal()  { renderQueueList(); document.getElementById('offlineModal').classList.remove('hidden'); }
function closeOfflineModal() { document.getElementById('offlineModal').classList.add('hidden'); }
function renderQueueList() {
  const q = getQueue(), el = document.getElementById('queueList');
  if(!q.length) { el.innerHTML = '<p style="color:var(--muted);font-size:13px;text-align:center;padding:12px;">No pending submissions.</p>'; return; }
  el.innerHTML = q.map((item,i) => {
    const t = new Date(item._queuedAt);
    return `<div class="q-item"><div class="qi-info"><div>Survey #${i+1} — ${item.constituency||'?'}, ${item.ward||'?'}</div><div class="qi-time">⏱ Queued: ${t.toLocaleDateString()} ${t.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</div></div><button class="qi-del" onclick="removeQueueItem('${item._queueId}')">🗑</button></div>`;
  }).join('');
}
function removeQueueItem(id) { if(!confirm('Remove this queued survey?')) return; dequeue(id); renderQueueList(); }
async function syncPendingNow() {
  if(!navigator.onLine) { alert('Still offline. Please connect first.'); return; }
  const q = getQueue(); if(!q.length) { closeOfflineModal(); return; }
  const btn = document.getElementById('syncNowBtn');
  const wrap = document.getElementById('syncProgressWrap'), bar = document.getElementById('syncProgressBar'), txt = document.getElementById('syncStatusText');
  btn.disabled = true; wrap.style.display = 'block';
  let done = 0, errs = [];
  for(const item of q) {
    try {
      txt.textContent = `Uploading ${done+1} of ${q.length}…`;
      bar.style.width = Math.round(done/q.length*100)+'%';
      const {_queuedAt, _queueId, ...fd} = item;
      fd.offlineQueuedAt = _queuedAt;
      await db.collection('responses').add(fd);
      dequeue(item._queueId); done++;
      bar.style.width = Math.round(done/q.length*100)+'%';
    } catch(e) { errs.push(item._queueId); }
  }
  btn.disabled = false; wrap.style.display = 'none'; renderQueueList(); refreshPendingUI();
  if(!errs.length) { alert(`✅ All ${done} survey(s) uploaded!`); closeOfflineModal(); }
  else alert(`⚠️ ${done} uploaded, ${errs.length} failed. Please retry.`);
}

/* ══ NETWORK ══ */
function updateNetworkBadge() {
  const b = document.getElementById('networkBadge');
  if(!navigator.onLine) {
    b.className = 'offline'; b.innerHTML = '📵 Offline — drafts saved locally'; b.style.display = 'flex';
  } else {
    b.className = 'online'; b.innerHTML = '✅ Back online'; b.style.display = 'flex';
    setTimeout(() => b.style.display = 'none', 3000);
    if(getQueue().length > 0) setTimeout(syncPendingNow, 1500);
  }
}
window.addEventListener('online',  updateNetworkBadge);
window.addEventListener('offline', updateNetworkBadge);

/* ══ LIVE GPS WATCH ══ */
let watchId = null, livePos = null;
function startGpsWatch() {
  if(!navigator.geolocation) return;
  document.getElementById('gpsStatusBar').style.display = 'block';
  setGpsDot('acquiring', 'Acquiring signal…', null);
  watchId = navigator.geolocation.watchPosition(
    pos => {
      livePos = pos;
      const acc = Math.round(pos.coords.accuracy), good = acc <= GPS_CONFIG.MAX_ACCURACY_METERS;
      setGpsDot(good?'good':'poor', `${pos.coords.latitude.toFixed(5)}, ${pos.coords.longitude.toFixed(5)}`, acc);
      updateLiveDistanceChip(pos.coords.latitude, pos.coords.longitude);
      updateLocationBox(pos);
    },
    () => setGpsDot('poor', 'GPS unavailable', null),
    { enableHighAccuracy: true, maximumAge: 5000 }
  );
}
function updateLocationBox(pos) {
  const box = document.getElementById('locationBox');
  if(!pos) { box.style.display = 'none'; return; }
  box.style.display = 'block';
  document.getElementById('locLat').textContent = pos.coords.latitude.toFixed(6);
  document.getElementById('locLng').textContent = pos.coords.longitude.toFixed(6);
  const acc = Math.round(pos.coords.accuracy), good = acc <= GPS_CONFIG.MAX_ACCURACY_METERS;
  const el = document.getElementById('locAcc');
  el.textContent = `±${acc} m`; el.className = 'loc-acc '+(good?'good':'poor');
}
function setGpsDot(state, text, accuracy) {
  document.getElementById('gpsDot').className = 'gps-dot '+state;
  let html = text;
  if(accuracy !== null) {
    const cls = accuracy <= GPS_CONFIG.MAX_ACCURACY_METERS ? 'good' : 'poor';
    html += ` &nbsp;·&nbsp; Accuracy: <span class="loc-acc ${cls}">±${accuracy} m</span>`;
  }
  document.getElementById('gpsStatusText').innerHTML = html;
}
function updateLiveDistanceChip(lat, lng) {
  const chip   = document.getElementById('gpsDistanceChip');
  const banner = document.getElementById('proximityLockBanner');
  const lockMsg = document.getElementById('proximityLockMsg');
  const btn = document.getElementById('submitBtn');
  const last = getResRecord();
  if(!last) { chip.style.display = 'none'; banner.style.display = 'none'; if(btn) btn.disabled = false; return; }
  const dist   = Math.round(haversineM(last.lat, last.lng, lat, lng));
  const distOk = dist >= GPS_CONFIG.MIN_DISTANCE_METERS;
  const timeOk = (Date.now() - last.ts) >= GPS_CONFIG.MIN_TIME_GAP_MS;
  chip.style.display = 'inline-block';
  if(distOk && timeOk) {
    chip.style.cssText += ';background:#dcfce7;color:#166534;border:1px solid #86efac';
    chip.textContent = '📍 Location OK';
  } else {
    chip.style.cssText += ';background:#fee2e2;color:#991b1b;border:1px solid #fca5a5';
    chip.textContent = distOk ? '⏱ Wait required' : '📍 Move to new location';
  }
  if(!distOk || !timeOk) {
    const wait = Math.ceil((GPS_CONFIG.MIN_TIME_GAP_MS - (Date.now() - last.ts)) / 1000);
    lockMsg.textContent = !distOk ? 'Please move to a different location before your next respondent.' : `Please wait ${wait} more second(s) before next submission.`;
    banner.style.display = 'block'; if(btn) btn.disabled = true;
  } else { banner.style.display = 'none'; if(btn) btn.disabled = false; }
}

/* ══ GPS GUARD MODAL ══ */
let pendingData = null;
function setStep(id, state, detail) {
  document.getElementById(id).className = state;
  const s = document.getElementById(id+'-detail'); if(s && detail) s.textContent = detail;
}
function setProgress(pct) { document.getElementById('gpsProgressBar').style.width = pct+'%'; }
function openGpsGuard(payload) {
  pendingData = payload;
  const isFirst = !getResRecord();
  ['step-acquire','step-accuracy','step-distance','step-time'].forEach(id => {
    document.getElementById(id).className = '';
    const s = document.getElementById(id+'-detail'); if(s) s.textContent = '';
  });
  document.getElementById('gpsGuardSubtitle').textContent = isFirst ? 'Recording your starting location — please wait' : 'Verifying GPS before submitting — please wait';
  const note = document.getElementById('firstSurveyNote');
  note.style.display = isFirst ? 'block' : 'none';
  document.getElementById('step-accuracy-detail').textContent = isFirst ? 'Skipped on first survey — any fix accepted' : `Requires ≤ ${GPS_CONFIG.MAX_ACCURACY_METERS} m accuracy`;
  document.getElementById('step-distance-detail').textContent = isFirst ? 'Skipped — no prior location to compare' : 'Must be 500 m from last survey';
  document.getElementById('step-time-detail').textContent = isFirst ? 'Skipped — no prior survey to compare' : 'Checking time since last survey';
  setProgress(0);
  document.getElementById('liveGpsCard').style.display = 'none';
  document.getElementById('gpsRetryBtn').style.display = 'none';
  document.getElementById('gpsCancelBtn').textContent = 'Cancel';
  document.getElementById('gpsGuardModal').classList.remove('hidden');
  runGpsChecks();
}
function cancelGpsCheck() {
  document.getElementById('gpsGuardModal').classList.add('hidden');
  const btn = document.getElementById('submitBtn'); btn.disabled = false; btn.textContent = '📍 Submit Survey';
  pendingData = null;
}
function retryGpsCheck() { document.getElementById('gpsRetryBtn').style.display = 'none'; runGpsChecks(); }
function failGpsGuard(stepId, msg) {
  setStep(stepId, 'fail', msg);
  document.getElementById('gpsRetryBtn').style.display = 'inline-block';
  const btn = document.getElementById('submitBtn'); btn.disabled = false; btn.textContent = '📍 Submit Survey';
}
function runGpsChecks() {
  if(!navigator.geolocation) { failGpsGuard('step-acquire', 'GPS not supported on this device.'); return; }
  if(livePos && livePos.coords.accuracy <= GPS_CONFIG.MAX_ACCURACY_METERS) {
    setStep('step-acquire', 'active', 'Using live GPS fix…'); setProgress(10);
    setTimeout(() => processGpsFix(livePos), 300); return;
  }
  setStep('step-acquire', 'active', 'Requesting high-accuracy GPS…'); setProgress(10);
  const highAccTimer = setTimeout(() => {
    setStep('step-acquire', 'active', 'High-accuracy timed out → trying network GPS…');
    navigator.geolocation.getCurrentPosition(pos => processGpsFix(pos), err => handleGpsError(err), { enableHighAccuracy: false, timeout: GPS_CONFIG.LOW_ACC_TIMEOUT_MS, maximumAge: GPS_CONFIG.MAX_AGE_MS });
  }, GPS_CONFIG.HIGH_ACC_TIMEOUT_MS);
  navigator.geolocation.getCurrentPosition(
    pos => { clearTimeout(highAccTimer); processGpsFix(pos); },
    err => {
      clearTimeout(highAccTimer);
      if(err.code === 3) {
        setStep('step-acquire', 'active', 'Switching to network GPS…');
        navigator.geolocation.getCurrentPosition(pos => processGpsFix(pos), err2 => handleGpsError(err2), { enableHighAccuracy: false, timeout: GPS_CONFIG.LOW_ACC_TIMEOUT_MS, maximumAge: GPS_CONFIG.MAX_AGE_MS });
      } else { handleGpsError(err); }
    },
    { enableHighAccuracy: true, timeout: GPS_CONFIG.HIGH_ACC_TIMEOUT_MS, maximumAge: GPS_CONFIG.MAX_AGE_MS }
  );
}
function handleGpsError(err) {
  const msgs = { 1:'Location permission denied.\nPlease allow location access in your browser settings and retry.', 2:'GPS signal unavailable. Please go outside and retry.', 3:'Location timed out. Please move to an open area and tap Retry.' };
  failGpsGuard('step-acquire', msgs[err.code] || err.message);
}
function processGpsFix(pos) {
  const lat = pos.coords.latitude, lng = pos.coords.longitude;
  const acc = Math.round(pos.coords.accuracy), now = Date.now();
  const isFirst = !getResRecord();
  document.getElementById('liveGpsCoordsDisplay').textContent = `${lat.toFixed(6)}, ${lng.toFixed(6)}`;
  document.getElementById('liveGpsAccDisplay').textContent = `Accuracy: ±${acc} m`;
  document.getElementById('liveGpsCard').style.display = 'block';
  setStep('step-acquire', 'done', `${lat.toFixed(5)}, ${lng.toFixed(5)}`);
  setProgress(30);
  if(isFirst) {
    setStep('step-accuracy', 'skipped', '✓ Skipped — first survey');
    setStep('step-distance', 'skipped', '✓ Skipped — no prior location');
    setStep('step-time',     'skipped', '✓ Skipped — no prior survey');
    setProgress(100);
    pendingData.gps = { lat, lng, accuracy: acc };
    pendingData.submittedAt = new Date().toISOString();
    pendingData.timestamp = firebase.firestore.FieldValue.serverTimestamp();
    pendingData.firstSurvey = true;
    submitOrEnqueue(pendingData, lat, lng, now); return;
  }
  setStep('step-accuracy', 'active', `Measured: ±${acc} m`); setProgress(50);
  if(acc > GPS_CONFIG.MAX_ACCURACY_METERS) { failGpsGuard('step-accuracy', `Accuracy too low (±${acc} m). Move outside and retry.`); return; }
  setStep('step-accuracy', 'done', `±${acc} m — OK`); setProgress(65);
  setStep('step-distance', 'active', 'Checking previous location…');
  const last = getResRecord();
  const dist = Math.round(haversineM(last.lat, last.lng, lat, lng));
  if(dist < GPS_CONFIG.MIN_DISTANCE_METERS) { failGpsGuard('step-distance', `Too close to last survey location (${dist} m). Please move at least ${GPS_CONFIG.MIN_DISTANCE_METERS} m away.`); return; }
  setStep('step-distance', 'done', `${dist} m from last survey — OK`); setProgress(80);
  setStep('step-time', 'active', 'Checking time since last survey…');
  const elapsed = now - last.ts;
  if(elapsed < GPS_CONFIG.MIN_TIME_GAP_MS) { const wait = Math.ceil((GPS_CONFIG.MIN_TIME_GAP_MS - elapsed) / 1000); failGpsGuard('step-time', `Too soon. Please wait ${wait} more second(s).`); return; }
  setStep('step-time', 'done', `${Math.round(elapsed/1000)}s since last survey — OK`); setProgress(100);
  pendingData.gps = { lat, lng, accuracy: acc };
  pendingData.submittedAt = new Date().toISOString();
  pendingData.timestamp = firebase.firestore.FieldValue.serverTimestamp();
  pendingData.firstSurvey = false;
  submitOrEnqueue(pendingData, lat, lng, now);
}

/* ══ SUBMIT / ENQUEUE ══ */
function submitOrEnqueue(data, lat, lng, ts) {
  if(!navigator.onLine) {
    document.getElementById('gpsGuardModal').classList.add('hidden');
    enqueue(data); saveResRecord(lat, lng, ts); clearDraft();
    showStatus('offline', '📵 <strong>Offline — survey saved locally.</strong><br>Will upload automatically when you reconnect.');
    resetForm(); pendingData = null; return;
  }
  db.collection('responses').add(data)
    .then(() => {
      saveResRecord(lat, lng, ts); clearDraft();
      document.getElementById('gpsGuardModal').classList.add('hidden');
      showStatus('success', '✓ Survey submitted successfully. Thank you!');
      resetForm(); pendingData = null;
      window.scrollTo({ top: 0, behavior: 'smooth' });
    })
    .catch(err => {
      document.getElementById('gpsGuardModal').classList.add('hidden');
      enqueue(data); saveResRecord(lat, lng, ts); clearDraft();
      showStatus('offline', `📵 <strong>Upload failed — saved locally.</strong><br>${err.message}`);
      resetForm(); pendingData = null;
    });
}
function showStatus(cls, html) { const s = document.getElementById('status'); s.className = 'status-msg '+cls; s.innerHTML = html; s.style.display = 'block'; }
function resetForm() {
  document.getElementById('surveyForm').reset();
  document.querySelectorAll('.gov-card').forEach(c => c.classList.remove('selected'));
  document.querySelectorAll('.conditional-field').forEach(f => f.classList.remove('visible'));
  updateWards();
  const btn = document.getElementById('submitBtn'); btn.disabled = false; btn.textContent = '📍 Submit Survey';
  if(livePos) updateLiveDistanceChip(livePos.coords.latitude, livePos.coords.longitude);
}

/* ══ GOVERNOR LOGIC ══ */
function selectGovernor(card) {
  document.querySelectorAll('.gov-card').forEach(c => c.classList.remove('selected'));
  card.classList.add('selected');
  card.querySelector('input[type="radio"]').checked = true;
  updateNegativeQuestions();
}
function updateNegativeQuestions() {
  const sel = document.querySelector('input[name="governor"]:checked');
  const mOpts = document.getElementById('notMbarireOptions'), mMsg = document.getElementById('notMbarireDisabledMsg');
  const kOpts = document.getElementById('notKivutiOptions'),  kMsg = document.getElementById('notKivutiDisabledMsg');
  const show = (opts, msg) => { opts.style.display = 'flex'; msg.style.display = 'none'; };
  const hide = (opts, msg) => { opts.style.display = 'none'; msg.style.display = 'block'; };
  if(!sel) { show(mOpts,mMsg); show(kOpts,kMsg); return; }
  const v = sel.value;
  if(v === 'Cecily Mbarire')  { hide(mOpts,mMsg); show(kOpts,kMsg); }
  else if(v === 'Lenny Kivuti') { show(mOpts,mMsg); hide(kOpts,kMsg); }
  else { show(mOpts,mMsg); show(kOpts,kMsg); }
}

/* ══ FORM SUBMIT ══ */
document.getElementById('surveyForm').addEventListener('submit', function(e) {
  e.preventDefault();
  const btn = document.getElementById('submitBtn');
  document.getElementById('status').style.display = 'none';
  btn.disabled = true; btn.textContent = 'Verifying location…';
  const r = JSON.parse(localStorage.getItem('ecs_researcher') || '{}');
  const data = {
    researcherName: r.name||'', researcherId: r.id||'',
    gender:       (document.querySelector('input[name="gender"]:checked')||{}).value||'',
    constituency:  document.getElementById('constituency').value,
    ward:          document.getElementById('ward').value,
    subLocation:   document.getElementById('subLocation').value,
    age:          (document.querySelector('input[name="age"]:checked')||{}).value||'',
    education:    (document.querySelector('input[name="education"]:checked')||{}).value||'',
    employment:   (document.querySelector('input[name="employment"]:checked')||{}).value||'',
    voter:        (document.querySelector('input[name="voter"]:checked')||{}).value||'',
    engageFrequency: (document.querySelector('input[name="engage"]:checked')||{}).value||'',
    engageNeverReason: document.getElementById('engageNeverReason').value,
    motivation:    getChecked(['b3a','b3b','b3c','b3d']),
    opinion:      (document.querySelector('input[name="opinion"]:checked')||{}).value||'',
    issues:        document.getElementById('issues').value,
    notDelivered:  getChecked(['b6a','b6b','b6c','b6d','b6e','b6f']),
    infoSources:   getChecked(['c1a','c1b','c1c','c1d','c1e','c1f','c1g']),
    transparency: (document.querySelector('input[name="transparency"]:checked')||{}).value||'',
    performance:  (document.querySelector('input[name="performance"]:checked')||{}).value||'',
    projectsAware: (document.querySelector('input[name="projectsAware"]:checked')||{}).value||'',
    projectsDelivered: document.getElementById('projectsDelivered').value,
    missedPromises: (document.querySelector('input[name="missedPromises"]:checked')||{}).value||'',
    projectsMissing: document.getElementById('projectsMissing').value,
    needs:         document.getElementById('needs').value,
    devAttention: (document.querySelector('input[name="devAttention"]:checked')||{}).value||'',
    svc_agriculture:   (document.querySelector('input[name="svc_agri"]:checked')||{}).value||'',
    svc_infrastructure:(document.querySelector('input[name="svc_infra"]:checked')||{}).value||'',
    svc_ecd:           (document.querySelector('input[name="svc_ecd"]:checked')||{}).value||'',
    svc_vtc:           (document.querySelector('input[name="svc_vtc"]:checked')||{}).value||'',
    svc_health:        (document.querySelector('input[name="svc_health"]:checked')||{}).value||'',
    svc_youth:         (document.querySelector('input[name="svc_youth"]:checked')||{}).value||'',
    svc_women:         (document.querySelector('input[name="svc_women"]:checked')||{}).value||'',
    svc_pwds:          (document.querySelector('input[name="svc_pwds"]:checked')||{}).value||'',
    svc_govtAccess:    (document.querySelector('input[name="svc_govtaccess"]:checked')||{}).value||'',
    smeObstacles:  getChecked(['f4a','f4b','f4c','f4d','f4e','f4f','f4g']),
    governor:     (document.querySelector('input[name="governor"]:checked')||{}).value||'',
    politicalParty:(document.querySelector('input[name="politicalParty"]:checked')||{}).value||'',
    voteReasons:   getChecked(['g2a','g2b','g2c','g2d','g2e','g2f','g2g','g2h','g2i','g2j','g2k']),
    notMbarireReasons: getChecked(['g3a','g3b','g3c','g3d','g3e','g3f','g3g','g3h','g3i']),
    adviceMbarire: document.getElementById('adviceMbarire').value,
    notKivutiReasons:  getChecked(['g5a','g5b','g5c','g5d','g5e','g5f','g5g','g5h','g5i']),
    adviceKivuti:  document.getElementById('adviceKivuti').value,
    comments:      document.getElementById('comments').value,
    devOffice:    (document.querySelector('input[name="devOffice"]:checked')||{}).value||'',
    worship:      (document.querySelector('input[name="worship"]:checked')||{}).value||'',
    contact: {
      name:     document.getElementById('contactName').value,
      phone:    document.getElementById('contactPhone').value,
      followUp: (document.querySelector('input[name="followUp"]:checked')||{}).value||''
    },
    gps: null, submittedAt: null
  };
  openGpsGuard(data);
});

/* ══ CONSENT & RESEARCHER FLOW ══ */
function toggleProceed() { document.getElementById('proceedBtn').disabled = !document.getElementById('consentCheck').checked; }
function declineConsent() { document.getElementById('consentModal').classList.add('hidden'); document.getElementById('declinedMsg').style.display = 'block'; }
function proceedAfterConsent() {
  if(!document.getElementById('consentCheck').checked) return;
  document.getElementById('consentModal').classList.add('hidden');
  checkResearcher();
}
function checkResearcher() {
  const s = localStorage.getItem('ecs_researcher');
  if(s) { try { const r = JSON.parse(s); if(r.name && r.id) { showSurvey(r.name, r.id); return; } } catch(e) {} }
  document.getElementById('researcherModal').classList.remove('hidden');
}
function saveResearcher() {
  const name = document.getElementById('resName').value.trim();
  const id   = document.getElementById('resId').value.trim();
  let ok = true;
  document.getElementById('resNameErr').style.display = name ? 'none' : 'block'; if(!name) ok = false;
  document.getElementById('resIdErr').style.display   = id   ? 'none' : 'block'; if(!id)   ok = false;
  if(!ok) return;
  localStorage.setItem('ecs_researcher', JSON.stringify({ name, id }));
  document.getElementById('researcherModal').classList.add('hidden');
  showSurvey(name, id);
}
function showSurvey(name, id) {
  document.getElementById('bannerName').textContent = name;
  document.getElementById('bannerId').textContent   = id;
  document.getElementById('researcherBanner').style.display = 'flex';
  document.getElementById('surveyWrapper').style.display    = 'block';
  startGpsWatch(); attachAutosave(); refreshPendingUI(); checkForDraft();
  if(navigator.onLine && getQueue().length > 0) syncPendingNow();
  window.scrollTo({ top: 0 });
}
function changeResearcher() {
  if(watchId !== null) { navigator.geolocation.clearWatch(watchId); watchId = null; }
  localStorage.removeItem('ecs_researcher'); localStorage.removeItem('ecs_gps_last');
  document.getElementById('resName').value = ''; document.getElementById('resId').value = '';
  document.getElementById('surveyWrapper').style.display    = 'none';
  document.getElementById('researcherBanner').style.display = 'none';
  document.getElementById('gpsStatusBar').style.display     = 'none';
  document.getElementById('draftBar').style.display         = 'none';
  document.getElementById('researcherModal').classList.remove('hidden');
}

if(!navigator.onLine) {
  const b = document.getElementById('networkBadge');
  b.className = 'offline'; b.innerHTML = '📵 Offline — drafts saved locally'; b.style.display = 'flex';
}

/* ══ DOWNLOAD QUESTIONNAIRE ══ */
function downloadQuestionnaire() {
  const html = `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><title>Embu County Survey — Questionnaire ECS-2025</title>
<style>
*{box-sizing:border-box;margin:0;padding:0}body{font-family:Arial,sans-serif;font-size:12px;color:#111;background:#fff}
@page{size:A4;margin:18mm 16mm 18mm 16mm}
.cover{text-align:center;padding:40px 20px 30px;border-bottom:3px solid #0f2547;margin-bottom:24px}
.cover h1{font-size:20px;color:#0f2547;letter-spacing:1.5px;text-transform:uppercase;margin-bottom:6px}
.cover .sub{font-size:12px;color:#555;font-style:italic;margin-bottom:10px}
.cover .badge{display:inline-block;background:#0f2547;color:white;padding:3px 16px;border-radius:20px;font-size:10px;letter-spacing:1px;text-transform:uppercase}
.cover .meta{margin-top:18px;font-size:11px;color:#444;line-height:2.2}
.cover .meta span{font-weight:700}
.section-hd{background:#0f2547;color:white;padding:7px 14px;border-radius:5px;margin:20px 0 12px;display:flex;align-items:center;gap:10px;page-break-after:avoid}
.section-hd .lt{background:rgba(255,255,255,.2);border-radius:50%;width:24px;height:24px;display:flex;align-items:center;justify-content:center;font-weight:700;font-size:12px;flex-shrink:0}
.section-hd h2{font-size:12px;text-transform:uppercase;letter-spacing:.5px;font-weight:600}
.q{margin-bottom:16px;page-break-inside:avoid}
.ql{font-weight:700;font-size:12px;color:#0f2547;margin-bottom:5px}
.ql .n{color:#1255cc;margin-right:4px}
.opts{display:flex;flex-wrap:wrap;gap:6px 10px;margin-top:5px}
.opt{display:flex;align-items:center;gap:5px;font-size:11px;color:#333}
.box{width:13px;height:13px;border:1.5px solid #555;border-radius:2px;flex-shrink:0}
.circ{width:13px;height:13px;border:1.5px solid #555;border-radius:50%;flex-shrink:0}
.wl{border-bottom:1px solid #bbb;height:20px;margin-bottom:5px}
.wls{margin-top:6px}
table{width:100%;border-collapse:collapse;font-size:11px;margin-top:6px}
th,td{border:1px solid #ccc;padding:6px 8px;text-align:center}
th{background:#e8f0fc;color:#0f2547;font-size:10px}
td:first-child{text-align:left;font-weight:600}
tr:nth-child(even) td{background:#fafafa}
.footer{margin-top:30px;border-top:1px solid #ccc;padding-top:8px;font-size:9px;color:#888;text-align:center}
.pb{page-break-before:always}
</style></head><body>
<div class="cover">
<h1>Embu County Political Survey</h1>
<div class="sub">Research Questionnaire — ECS-2025</div>
<div class="badge">Confidential · For Research Purposes Only</div>
<div class="meta">
<div>Researcher Name: <span>_______________________________</span> &nbsp;&nbsp; ID: <span>_______________</span></div>
<div>Date: <span>__________________</span> &nbsp;&nbsp; Time: <span>____________</span></div>
<div>Constituency: <span>__________________</span> &nbsp;&nbsp; Ward: <span>__________________</span></div>
<div>Sub-Location / Village: <span>_________________________________</span></div>
<div>Respondent Code: <span>_______________</span></div>
</div>
</div>

<div class="section-hd"><div class="lt">A</div><h2>Section A — Respondent Demographics</h2></div>
<div class="q"><div class="ql"><span class="n">A1.</span> Gender</div><div class="opts"><div class="opt"><div class="circ"></div> Male</div><div class="opt"><div class="circ"></div> Female</div><div class="opt"><div class="circ"></div> Prefer not to say</div></div></div>
<div class="q"><div class="ql"><span class="n">A2.</span> Constituency</div><div class="opts"><div class="opt"><div class="circ"></div> Manyatta</div><div class="opt"><div class="circ"></div> Runyenjes</div><div class="opt"><div class="circ"></div> Mbeere North</div><div class="opt"><div class="circ"></div> Mbeere South</div></div></div>
<div class="q"><div class="ql"><span class="n">A3.</span> Ward</div><div class="wls"><div class="wl"></div></div></div>
<div class="q"><div class="ql"><span class="n">A4.</span> Sub-Location / Village</div><div class="wls"><div class="wl"></div></div></div>
<div class="q"><div class="ql"><span class="n">A5.</span> Age Category</div><div class="opts"><div class="opt"><div class="circ"></div> 18–35</div><div class="opt"><div class="circ"></div> 36–60</div><div class="opt"><div class="circ"></div> 61+</div></div></div>
<div class="q"><div class="ql"><span class="n">A6.</span> Highest Education Level</div><div class="opts"><div class="opt"><div class="circ"></div> None</div><div class="opt"><div class="circ"></div> Primary</div><div class="opt"><div class="circ"></div> Secondary</div><div class="opt"><div class="circ"></div> Tertiary (College/University)</div></div></div>
<div class="q"><div class="ql"><span class="n">A7.</span> Employment Status</div><div class="opts"><div class="opt"><div class="circ"></div> Employed</div><div class="opt"><div class="circ"></div> Self-Employed</div><div class="opt"><div class="circ"></div> Unemployed</div></div></div>

<div class="section-hd"><div class="lt">B</div><h2>Section B — Political Participation &amp; Governance</h2></div>
<div class="q"><div class="ql"><span class="n">B1.</span> Are you a registered voter?</div><div class="opts"><div class="opt"><div class="circ"></div> Yes</div><div class="opt"><div class="circ"></div> No</div></div></div>
<div class="q"><div class="ql"><span class="n">B2.</span> How often do you engage in politics?</div><div class="opts"><div class="opt"><div class="circ"></div> Frequently</div><div class="opt"><div class="circ"></div> Occasionally</div><div class="opt"><div class="circ"></div> Rarely</div><div class="opt"><div class="circ"></div> Never</div></div></div>
<div class="q"><div class="ql"><span class="n">B2.1.</span> If Never — please explain:</div><div class="wls"><div class="wl"></div><div class="wl"></div></div></div>
<div class="q"><div class="ql"><span class="n">B2.2.</span> What motivates your participation? (tick all)</div><div class="opts"><div class="opt"><div class="box"></div> Community development</div><div class="opt"><div class="box"></div> Personal interests</div><div class="opt"><div class="box"></div> Civic duty</div><div class="opt"><div class="box"></div> Other</div></div></div>
<div class="q"><div class="ql"><span class="n">B3.</span> Do you feel your opinions are considered in governance?</div><div class="opts"><div class="opt"><div class="circ"></div> Yes</div><div class="opt"><div class="circ"></div> No</div><div class="opt"><div class="circ"></div> Not sure</div></div></div>
<div class="q"><div class="ql"><span class="n">B4.</span> What are the most pressing political issues in the county?</div><div class="wls"><div class="wl"></div><div class="wl"></div></div></div>
<div class="q"><div class="ql"><span class="n">B5.</span> Areas where the county government has NOT delivered: (tick all)</div><div class="opts"><div class="opt"><div class="box"></div> Agriculture</div><div class="opt"><div class="box"></div> Infrastructure</div><div class="opt"><div class="box"></div> Investments</div><div class="opt"><div class="box"></div> SMEs</div><div class="opt"><div class="box"></div> Health</div><div class="opt"><div class="box"></div> Education</div></div></div>

<div class="section-hd"><div class="lt">C</div><h2>Section C — Political Information &amp; Transparency</h2></div>
<div class="q"><div class="ql"><span class="n">C1.</span> How do you usually get political information? (tick all)</div><div class="opts"><div class="opt"><div class="box"></div> Radio</div><div class="opt"><div class="box"></div> TV</div><div class="opt"><div class="box"></div> Newspapers</div><div class="opt"><div class="box"></div> Social Media</div><div class="opt"><div class="box"></div> Word of Mouth</div><div class="opt"><div class="box"></div> Barazas</div><div class="opt"><div class="box"></div> Other</div></div></div>
<div class="q"><div class="ql"><span class="n">C2.</span> Rate the transparency of the government:</div><div class="opts"><div class="opt"><div class="circ"></div> Excellent</div><div class="opt"><div class="circ"></div> Good</div><div class="opt"><div class="circ"></div> Fair</div><div class="opt"><div class="circ"></div> Poor</div></div></div>

<div class="section-hd"><div class="lt">D</div><h2>Section D — Project Delivery Assessment</h2></div>
<div class="q"><div class="ql"><span class="n">D1.</span> Rate the performance of the current county government:</div><div class="opts"><div class="opt"><div class="circ"></div> Excellent</div><div class="opt"><div class="circ"></div> Good</div><div class="opt"><div class="circ"></div> Fair</div><div class="opt"><div class="circ"></div> Poor</div></div></div>
<div class="q"><div class="ql"><span class="n">D2.</span> Are you aware of any projects delivered by the county government in your area?</div><div class="opts"><div class="opt"><div class="circ"></div> Yes</div><div class="opt"><div class="circ"></div> No</div></div><div style="margin-top:6px;font-size:11px;color:#555;">If Yes, name up to 2:</div><div class="wls"><div class="wl"></div><div class="wl"></div></div></div>
<div class="q"><div class="ql"><span class="n">D3.</span> Are there any projects that were promised but not delivered?</div><div class="opts"><div class="opt"><div class="circ"></div> Yes</div><div class="opt"><div class="circ"></div> No</div></div><div style="margin-top:6px;font-size:11px;color:#555;">If Yes, name up to 2:</div><div class="wls"><div class="wl"></div><div class="wl"></div></div></div>
<div class="q"><div class="ql"><span class="n">D4.</span> List your top 5 priority needs in your ward:</div><div class="wls"><div class="wl"></div><div class="wl"></div><div class="wl"></div><div class="wl"></div><div class="wl"></div></div></div>

<div class="section-hd pb"><div class="lt">E</div><h2>Section E — Service Delivery Rating</h2></div>
<div class="q"><div class="ql"><span class="n">E1.</span> How often do you pay attention to development activities in Embu County?</div><div class="opts"><div class="opt"><div class="circ"></div> Always</div><div class="opt"><div class="circ"></div> Often</div><div class="opt"><div class="circ"></div> Sometimes</div><div class="opt"><div class="circ"></div> Rarely</div><div class="opt"><div class="circ"></div> Never</div></div></div>
<div class="q"><div class="ql"><span class="n">E2.</span> Rate how the county government has performed in the following areas:</div>
<table><thead><tr><th style="text-align:left;width:35%">Service Area</th><th>Not Sure</th><th>Very Poor</th><th>Poor</th><th>Fair</th><th>Good</th><th>Excellent</th></tr></thead><tbody>
<tr><td>Agriculture Services</td><td></td><td></td><td></td><td></td><td></td><td></td></tr>
<tr><td>Infrastructure (Roads &amp; Markets)</td><td></td><td></td><td></td><td></td><td></td><td></td></tr>
<tr><td>Education — ECD / ECDE</td><td></td><td></td><td></td><td></td><td></td><td></td></tr>
<tr><td>Education — VTCs</td><td></td><td></td><td></td><td></td><td></td><td></td></tr>
<tr><td>Health Services</td><td></td><td></td><td></td><td></td><td></td><td></td></tr>
<tr><td>Youth Empowerment (SIGPs)</td><td></td><td></td><td></td><td></td><td></td><td></td></tr>
<tr><td>Women Empowerment (SIGPs)</td><td></td><td></td><td></td><td></td><td></td><td></td></tr>
<tr><td>PWDs Support (SIGPs)</td><td></td><td></td><td></td><td></td><td></td><td></td></tr>
<tr><td>Access to Gov't Services</td><td></td><td></td><td></td><td></td><td></td><td></td></tr>
</tbody></table></div>

<div class="section-hd"><div class="lt">F</div><h2>Section F — Business &amp; Economic Issues</h2></div>
<div class="q"><div class="ql"><span class="n">F1.</span> Challenges faced by business people / SMEs in Embu County: (tick all)</div><div class="opts"><div class="opt"><div class="box"></div> Finance / Capital</div><div class="opt"><div class="box"></div> High Taxes</div><div class="opt"><div class="box"></div> Limited Market</div><div class="opt"><div class="box"></div> Lack of Government Support</div><div class="opt"><div class="box"></div> Competition</div><div class="opt"><div class="box"></div> Poor Infrastructure</div><div class="opt"><div class="box"></div> Other</div></div></div>

<div class="section-hd"><div class="lt">G</div><h2>Section G — Political Sentiment</h2></div>
<div class="q"><div class="ql"><span class="n">G1.</span> Who would you vote for as Governor?</div><div class="opts"><div class="opt"><div class="circ"></div> Cecily Mbarire</div><div class="opt"><div class="circ"></div> Lenny Kivuti</div><div class="opt"><div class="circ"></div> A New Face</div><div class="opt"><div class="circ"></div> Not Yet Decided</div></div></div>
<div class="q"><div class="ql"><span class="n">G2.</span> Which political party do you identify with?</div><div class="opts"><div class="opt"><div class="circ"></div> BDEP</div><div class="opt"><div class="circ"></div> UDA</div><div class="opt"><div class="circ"></div> Jubilee</div><div class="opt"><div class="circ"></div> DP</div><div class="opt"><div class="circ"></div> DCP</div><div class="opt"><div class="circ"></div> Other</div><div class="opt"><div class="circ"></div> None</div></div></div>
<div class="q"><div class="ql"><span class="n">G3.</span> Why would you vote for that person? (tick all)</div><div class="opts"><div class="opt"><div class="box"></div> Track record</div><div class="opt"><div class="box"></div> Development agenda</div><div class="opt"><div class="box"></div> Trustworthy</div><div class="opt"><div class="box"></div> Leadership qualities</div><div class="opt"><div class="box"></div> Community connection</div><div class="opt"><div class="box"></div> Communication</div><div class="opt"><div class="box"></div> Proven delivery</div><div class="opt"><div class="box"></div> Party affiliation</div><div class="opt"><div class="box"></div> New ideas</div><div class="opt"><div class="box"></div> Personal help</div><div class="opt"><div class="box"></div> Other</div></div></div>
<div class="q"><div class="ql"><span class="n">G4.</span> Reasons for NOT voting for Cecily Mbarire: (tick all)</div><div class="opts"><div class="opt"><div class="box"></div> Poor performance</div><div class="opt"><div class="box"></div> Corruption</div><div class="opt"><div class="box"></div> Untrustworthy</div><div class="opt"><div class="box"></div> Tribal favoritism</div><div class="opt"><div class="box"></div> Party affiliation</div><div class="opt"><div class="box"></div> Weak leadership</div><div class="opt"><div class="box"></div> No vision</div><div class="opt"><div class="box"></div> Land issues</div><div class="opt"><div class="box"></div> Other</div></div></div>
<div class="q"><div class="ql"><span class="n">G5.</span> Advice to Cecily Mbarire:</div><div class="wls"><div class="wl"></div><div class="wl"></div></div></div>
<div class="q"><div class="ql"><span class="n">G6.</span> Reasons for NOT voting for Lenny Kivuti: (tick all)</div><div class="opts"><div class="opt"><div class="box"></div> Poor performance</div><div class="opt"><div class="box"></div> Corruption</div><div class="opt"><div class="box"></div> Untrustworthy</div><div class="opt"><div class="box"></div> Tribal favoritism</div><div class="opt"><div class="box"></div> Party affiliation</div><div class="opt"><div class="box"></div> Weak leadership</div><div class="opt"><div class="box"></div> No vision</div><div class="opt"><div class="box"></div> Land issues</div><div class="opt"><div class="box"></div> Other</div></div></div>
<div class="q"><div class="ql"><span class="n">G7.</span> Advice to Lenny Kivuti:</div><div class="wls"><div class="wl"></div><div class="wl"></div></div></div>
<div class="q"><div class="ql"><span class="n">G8.</span> Additional comments on the political state of Embu County:</div><div class="wls"><div class="wl"></div><div class="wl"></div></div></div>

<div class="section-hd pb"><div class="lt">H</div><h2>Section H — Civic &amp; Community Perspectives</h2></div>
<div class="q"><div class="ql"><span class="n">H1.</span> Which office contributes most to development in your area?</div><div class="opts"><div class="opt"><div class="circ"></div> President</div><div class="opt"><div class="circ"></div> Governor</div><div class="opt"><div class="circ"></div> MP</div><div class="opt"><div class="circ"></div> MCA</div><div class="opt"><div class="circ"></div> Senator</div><div class="opt"><div class="circ"></div> Women Rep</div></div></div>
<div class="q"><div class="ql"><span class="n">H2.</span> How often do you attend places of worship (church / mosque)?</div><div class="opts"><div class="opt"><div class="circ"></div> Regularly</div><div class="opt"><div class="circ"></div> Rarely</div><div class="opt"><div class="circ"></div> Never</div></div></div>

<div class="section-hd"><div class="lt">I</div><h2>Section I — Optional Contact Details</h2></div>
<div class="q"><div class="ql">Full Name (optional):</div><div class="wls"><div class="wl"></div></div></div>
<div class="q"><div class="ql">Phone Number (optional):</div><div class="wls"><div class="wl"></div></div></div>
<div class="q"><div class="ql">Consent to be contacted for follow-up?</div><div class="opts"><div class="opt"><div class="circ"></div> Yes</div><div class="opt"><div class="circ"></div> No</div></div></div>

<div class="footer">Embu County Political Survey — ECS-2025 &nbsp;|&nbsp; Confidential — For Research Purposes Only &nbsp;|&nbsp; All responses are anonymous and handled in accordance with data protection laws.</div>
</body></html>`;
  const blob = new Blob([html], { type: 'text/html;charset=utf-8' });
  const url  = URL.createObjectURL(blob);
  const a    = Object.assign(document.createElement('a'), { href: url, download: 'ECS-2025-Questionnaire.html' });
  document.body.appendChild(a); a.click(); document.body.removeChild(a); URL.revokeObjectURL(url);
  setTimeout(() => alert('✅ Questionnaire downloaded!\n\nTo save as PDF:\n1. Open the file in your browser\n2. Press Ctrl+P (Cmd+P on Mac)\n3. Set destination to "Save as PDF"\n4. Click Save'), 400);
}
</script>
</body>
</html>
